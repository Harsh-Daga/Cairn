# Gamma — go-to-market

Positioning: "Git for inference" — provenance over polish.

- Primary user: staff engineer debugging agent runs.
- Wedge: `ingest` → `render` in under 60 seconds on existing repos.
- Pricing TBD; open source core with optional team sync bundles.

Competitors: cloud observability SaaS (account required), raw JSONL grep (no graph).

Recommendation: ship CLI + HTML bundle before IDE plugins.
