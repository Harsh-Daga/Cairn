# Homebrew distribution

Homebrew installation is not a supported Cairn distribution path. This repository intentionally
does not ship an untested formula or a placeholder checksum.

Use the documented `uv tool install cairn-workspace` path. A future Homebrew path must have a real
maintained tap, immutable source checksum, macOS install/upgrade/uninstall CI, and matching release
ownership before this status changes.
