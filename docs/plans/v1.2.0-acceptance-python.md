# T08-01 Python acceptance report

Date: 2026-07-19  
Package version under test: `1.1.1` (unreleased 1.2.0 tree)

## Commands

```bash
uv run ruff check .
uv run ruff format --check .
uv run mypy --strict server
uv run pytest -q
```

## Results

| Gate | Result |
|------|--------|
| Ruff lint | pass |
| Ruff format | pass |
| mypy `--strict server` (265 files) | pass |
| pytest | **477 passed** |

## Coverage vs §8.1 matrix

| Area | Evidence (representative tests) |
|------|----------------------------------|
| Analyzers / detectors | `test_detector_*`, `test_analyze_*`, `test_insights` |
| Verification receipts / debt | `test_verification_receipt` |
| Corrections / handoff | `test_corrections_handoff` |
| Regression artifacts | `test_regression_artifacts` |
| Policy | `test_policy` |
| API contracts | `test_api`, `test_generated_api`, `test_sse` |
| Action parity | `test_parity` (45 registered actions) |
| CLI surface | `test_cli_*`, `test_cli_module_boundaries` |
| MCP | `test_mcp*` |
| Migrations / backup | `test_store` (pre-1.2 → 0013) |
| Static export / Pages | `test_static_export` |
| No-network default | `test_no_network` |
| Resource / jobs / watcher | `test_resources`, `test_jobs`, `test_watcher`, `test_circuit_breakers` |
| Archive / egress / reference | `test_archive`, `test_egress`, `test_reference_mode` |
| Docs / examples / generated | `test_docs`, `test_examples`, `test_generated_api` |
| Package / release scripts | `test_release_*`, `test_coverage_gate` |

## Fixes applied during this gate

- Synced action/CLI/migration expectations with Phase 5–6 surfaces.
- Regenerated OpenAPI/UI types + CLI docs.
- Strict-typing fixes (policy loop narrowing, lifecycle strip mode, action write closures, etc.).
- Format/import cleanup across server/tests.

## Gaps / follow-ons

- T08-02 UI Vitest/component matrix still pending.
- T08-03 Playwright multi-browser journeys still pending.
- T08-04 performance matrix still pending.
- Hypothesis property coverage exists for filters; expand opportunistically, not as a release blocker when behavior suites already pass.
