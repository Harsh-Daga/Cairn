# T08-02 UI Vitest acceptance report

Date: 2026-07-19  
UI package version: `1.1.1`

## Commands

```bash
npm --prefix ui run typecheck
npm --prefix ui run lint
npm --prefix ui run format:check
npm --prefix ui run check:api
npm --prefix ui test
```

## Results

| Gate | Result |
|------|--------|
| TypeScript | pass |
| ESLint (`--max-warnings 0`) | pass |
| Prettier | pass (12 files reformatted this wave) |
| OpenAPI/UI type drift | pass |
| Vitest | **93 passed** / 26 files |

## Coverage vs §8.2 matrix

| Area | Evidence |
|------|----------|
| Time-range / URL state | `test/timeRange.test.ts`, palette time-range commands |
| Filter grammar | `test/filterQuery.test.tsx` (+ generated operators) |
| Theme bootstrap/persistence | `test/theme.test.ts` |
| Tables / keyboard | `components/ui/primitives.test.tsx`, chart keyboard inspect |
| Dialogs / overlays | Settings rebuild confirm, palette, primitives |
| Formatters / estimate-confidence | primitives + `test/ui.test.tsx` estimated chip |
| Receipts / corrections / review risk | `sessionDetailViews` receipt+corrections test; filter `risk:` / unavailable claim |
| Resource & Privacy shields | `sessionDetailViews` four-shield independence; Overview shields |
| Errors / empty states | first-run, static API uncaptured filters, ErrorBoundary |
| Static API | `test/staticApi.test.ts` |
| Domain hooks / pages | overview, context, behavior, optimize, guard, insights, live, settings |

## Fixes this wave

- Prettier formatting for 12 drifted UI files.
- Added Vitest coverage for Session Receipt + Corrections honesty (no invented claims; ranking forbidden).

## Follow-ons

- T08-03 Playwright multi-browser journeys (mandatory).
- T08-04 performance matrix.
