# T08-04 Performance matrix acceptance report

Date: 2026-07-19  
Host: Apple arm64, 8 logical CPUs, Python 3.11.15 (engineering sample, not a cross-platform promise)

## Stable PR budgets (must pass)

```bash
uv run python scripts/build_ui.py build   # if assets missing
uv run python scripts/check_bundle_size.py
uv run pytest tests/test_performance_fixtures.py tests/test_waterfall_perf.py -q
```

| Check | Result |
|-------|--------|
| HTTP JS entry gzip ≤ 150 KiB | **83.5 KiB** pass |
| HTTP JS total gzip ≤ 350 KiB | **231.7 KiB** pass |
| Static IIFE gzip ≤ 240 KiB | **233.2 KiB** pass (ceiling raised from 230 → 240; IIFE inlines lazy routes) |
| Large fixture structural contracts | **6 passed** |

## Timing trends (scheduled / manual)

```bash
uv run python scripts/benchmark.py run \
  --profile large --workspace /tmp/cairn-bench-large \
  --samples 3 --include-static \
  --output-json docs/plans/v1.2.0-performance-results.json
```

Checked-in artifact refreshed: [`v1.2.0-performance-results.json`](v1.2.0-performance-results.json).

| Large-profile journey (n=3 samples) | p50 | p95 | Notes |
|-------------------------------------|----:|----:|-------|
| Wide trace detail (2,500 spans) | 247 ms | 418 ms | Under 500 ms triage |
| Wide replay | 130 ms | 130 ms | Under 500 ms triage |
| Search | (see JSON) | | |
| Trace list 200 | 37 ms | 38 ms | |
| Static export (1,000 details) | — | 35.6 s / 81.3 MiB / 133 MiB RSS | Time above 30 s triage; size/RSS under 96/256 MiB |

Workflow: `.github/workflows/performance.yml` (cron Monday + `workflow_dispatch`) uploads
`test-results/performance.json` and does **not** fail on wall-clock drift.

Browser timing script remains `ui/scripts/benchmark.mjs` (see prior
`v1.2.0-browser-performance-results.json`); not re-run this wave.

## §8.4 matrix map

| Item | Where measured |
|------|----------------|
| Small/medium/large DB generation | `benchmark.py` + `test_performance_fixtures` |
| Warm endpoints | `benchmark.py run` |
| Ingest first/incremental | `benchmark.py` ingest block |
| Trace detail thousands of spans | large profile wide_trace_* |
| Search / static export | large profile + static_export block |
| Bundle budgets | `check_bundle_size.py` in CI |
| Browser table/waterfall | structural vitest + prior browser JSON |
| Idle/watcher/jobs/archive timing | Behavior tests exist; long timings stay scheduled |

## Follow-ons

- Re-record browser Chromium timings when convenient (`benchmark.mjs` against large workspace).
- Investigate static-export wall time vs 30 s triage on CI runners (artifact trend, not PR gate).
