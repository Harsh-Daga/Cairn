# Cairn v1.2.0 final evidence report (FIN-02)

Date: 2026-07-19  
Working tree package version: **1.1.1** (1.2.0 not tagged or published)  
Tracker: [`v1.2.0-implementation.md`](v1.2.0-implementation.md)  
Canonical specs: [`v1.2.0-revamp-prompt.md`](v1.2.0-revamp-prompt.md), [`v1.2.0-execution-prompt.md`](v1.2.0-execution-prompt.md)

**Verdict:** The 1.2.0 *implementation program* is ready for a maintainer release PR that bumps
version sources and cuts an annotated tag. This report does **not** declare a public PyPI/GitHub
Release of 1.2.0 — that remains a maintainer-only action after remote settings and version bump.

---

## 1. Executive summary

Cairn’s local-first coding-agent control plane was extended across contracts, evidence domains
(receipts, regressions, corrections, policy, archive), resource/privacy shield controls, docs,
examples, and a single local release gate. Acceptance evidence:

| Gate | Evidence |
|------|----------|
| Python | 477 passed — [acceptance-python](v1.2.0-acceptance-python.md) |
| UI Vitest | 93 passed — [acceptance-ui](v1.2.0-acceptance-ui.md) |
| Playwright | 91 passed — [acceptance-browser](v1.2.0-acceptance-browser.md) |
| Performance | Structural budgets + refreshed large JSON — [acceptance-performance](v1.2.0-acceptance-performance.md) |
| Release gate | Full `release_check.py` green — [acceptance-release-gate](v1.2.0-acceptance-release-gate.md) |

Honest open / partial P1 rows: T06-06 CAS deferred via ADR-0013 (assessment tooling only);
T03-02 axe/E2E done, manual AT receipt still blank. Closed with honesty limits: T05-10
record-from-trace run/compare; T06-09 typed restore UI; T06-10 accelerated soak v2. Hot-path analytics
aggregates (tools/files/agents/compare/usage/overview money/tail) use hard caps + truncation
notes; some SQL `GROUP BY` paths still scan then limit. CI packaging job is named for packaging
only; full `release_check` remains the local release gate. All EXP/RES stay deferred P2.

---

## 2. Per-phase implementation summary

| Phase | Outcome |
|-------|---------|
| 0 Trust foundations | Docs claim suite, SECURITY/threat model, community files, packaging, API bounds, static scrub, auth, permissions, no-network, migrations, ingest privacy |
| 1 CI/security/release | Frozen multi-Python CI, UI gates, mandatory Playwright, coverage ratchet, CodeQL/Scorecard, tag-driven publish+SBOM+provenance, reproducibility |
| 2 Architecture | Payload/CLI/demo splits, OpenAPI→TS, time ranges, query indexes, config system, scale fixtures |
| 3 UI foundations | Theme tokens, primitives, charts, shell IA, first-run, responsive/a11y policy |
| 4 Product surfaces | Overview→Recap pages with shields, filters, compare, guard, optimize, live |
| 5 Evidence | Receipts, regressions, corrections/handoff, policy/review, MCP/CLI evidence, session HTML, evidence closure |
| 6 Resource/privacy | Collection modes, jobs/watcher, storage modes, lifecycle, pricing, archive, egress, circuit breakers, reference mode |
| 7 Docs/examples | README, required docs, Pages contract, examples, CONTRIBUTING |
| 8 Acceptance | Python/UI/browser/perf reports |
| FIN | Single `release_check` gate + this report |

---

## 3. Before/after screenshots and performance table

**Screenshots / baselines**

- Product media: `docs/assets/hero.gif`, `session-detail.png`, `optimize-verdict.png`
- Layout baselines: `docs/assets/v1.2/baseline/overview-{desktop,tablet,mobile}.png`

**Backend large profile (2026-07-19 local sample)** — see
[`v1.2.0-performance-results.json`](v1.2.0-performance-results.json):

| Journey | p50 | p95 |
|---------|----:|----:|
| Wide detail (2,500 spans) | 247 ms | 418 ms |
| Wide replay | 130 ms | 130 ms |
| Trace list 200 | 37 ms | 38 ms |
| Static export (1k details) | 35.6 s | 81 MiB out / 133 MiB RSS |

**Stable PR budgets:** HTTP entry ≤150 KiB, HTTP total ≤350 KiB, static IIFE ≤240 KiB (raised from
230). Browser timing sample remains
[`v1.2.0-browser-performance-results.json`](v1.2.0-browser-performance-results.json).

---

## 4. Compatibility report

- Public CLI entry `server.cli:app`, action registry, MCP tool names, and adapter entry points
  preserved; new surfaces are additive (`archive`, receipt/regression/privacy actions).
- Legacy `days` query params remain; custom ranges use ADR-01 half-open UTC bounds.
- Static export declares unsupported queries rather than inventing live parity (ADR-02).
- OpenAPI-generated UI types with committed compat snapshot (ADR-03).
- Package version in tree is still **1.1.1** until the release PR bumps all version sources.

---

## 5. Migration report

- Numbered SQL migrations through `0013_corrections_and_relabels.sql`.
- Pre-1.2 fixture upgrade + backup covered in `tests/test_store.py`.
- Migrations are transactional; doctor surfaces backup/repair paths.
- See [data-model](../data-model.md).

---

## 6. Privacy / network report

- Default bind loopback; default flows produce **empty** egress ledger (`test_no_network`).
- Opt-in reflector requires consent; destinations recorded without secrets ([egress](../egress.md)).
- Storage modes `reference`/`metrics`/`balanced`/`forensic`; strip/lifecycle warn-until-enabled.
- Static/session/archive exports scrub known credentials and absolute roots (best-effort, not proof).
- Git privacy / exclude approval and private path modes covered.

---

## 7. Accessibility report

- Theme system + AA contrast contracts (`ui/src/test/theme.test.ts`).
- Axe WCAG 2.2 AA on primary routes × dark/light ([acceptance-browser](v1.2.0-acceptance-browser.md)).
- Keyboard: skip link, palette focus trap, chart/table keyboard paths.
- Policy: [accessibility](../accessibility.md), [browser-support](../browser-support.md).
- Manual AT review remains a maintainer follow-on (not claimed as certification).

---

## 8. Test and coverage report

| Suite | Result |
|-------|--------|
| `uv run pytest -q` | 477 passed |
| UI Vitest | 93 passed |
| Playwright | 91 passed |
| Full `release_check.py` | passed (~7.5m) |

Coverage ratchet: `docs/plans/v1.2.0-coverage-baseline.json` + `scripts/check_coverage.py`
([testing](../testing.md)).

---

## 9. User-journey and differentiation report

Differentiating journeys exercised in demo/E2E:

- Overview shields + attention evidence → Sessions → Session Detail receipt/corrections/postmortem
- Compare difficulty cells; Optimize ledger/verdicts; Guard association language
- Static `file://` snapshot; Pages `/Cairn/` hosting contract
- CI gate / OTLP / MCP / archive examples under `examples/`

Research notes: [competitive landscape](../research/competitive-landscape.md),
[user needs](../research/user-needs.md).

---

## 10. Verification / receipt false-positive report

- Receipt v1 is deterministic from outcomes/spans; claim rows intentionally absent — absence ≠
  unsupported/contradicted ([verification](../verification.md)).
- Corrections prefer false negatives; local relabel overrides; ranking forbidden in payloads/UI.
- Policy findings are advisory observations (never “Cairn blocked”).
- Tests: `test_verification_receipt`, `test_corrections_handoff`, `test_policy`, UI receipt test.

---

## 11. Resource benchmark and storage-growth report

- Structural large-fixture budgets in CI; noisy timings on `performance.yml`.
- Circuit breakers / quarantine / soft disk budget / jobs / watcher behavior tests green.
- T06-09 Resource Center includes typed restore (dry-run first). T06-10 is accelerated soak v2.
- Details: [acceptance-performance](v1.2.0-acceptance-performance.md), [performance](../performance.md).

---

## 12. Local file-permission / privacy report

- Owner-only defaults for `.cairn/` trees; doctor `--repair-permissions`.
- Private writers used across export/config/lifecycle.
- Tests under doctor / private_files / git privacy / storage modes.

---

## 13. Untrusted telemetry / optional LLM / MCP security report

- ADR-09 boundaries; reflector preview/consent; MCP ledger `mode=ro`.
- Hostile/untrusted content treated as data in transcript/diff progressive rendering.
- MCP tools cannot mutate the ledger; consultations are privacy-safe markers.
- Threat model: [security/threat-model](../security/threat-model.md).

---

## 14. Data-portability, egress-ledger, and circuit-breaker report

- `cairn.archive.v1` export/inspect/import with dry-run and path-safe ZIP (ADR-10;
  `test_archive`).
- OTLP loss documented in [otlp](../otlp.md) / [archive](../archive.md).
- Egress ledger field-class only ([egress](../egress.md)).
- Circuit breakers + quarantine ([resource-shield](../resource-shield.md)).

---

## 15. Package / release / reproducibility / SLSA / OpenSSF readiness

- Tag-driven `publish.yml` with attestations, SBOM, SHA256SUMS, Trusted Publishing environment.
- `release_check.py` full local gate; CI uses `--packaging-only`.
- Reproducibility assessment: [reproducible-builds](../reproducible-builds.md).
- OSPS gap report (dated): [openssf-gap-report-2026-07-17](../security/openssf-gap-report-2026-07-17.md).
- **Do not claim a Scorecard/OSPS badge or SLSA level beyond what workflows actually enforce.**

---

## 16. Unresolved risks and intentionally deferred P2 work

**Open / unfinished / partial P1**

| ID | Item |
|----|------|
| T03-02 | Manual AT receipt still blank; axe/E2E automation landed |
| T06-06 | CAS deferred (ADR 0013); assessment tooling only |

**Closed this continuation (2026-07-19 honesty fixes)**

| Item |
|------|
| T05-10 regression run/compare from ingested traces (never executes commands) |
| T06-09 Resource Center backup list / dry-run / typed restore |
| T06-10 resource soak v2 idle / EventBus / jobs windows (still accelerated) |
| Tools page adopts shared `DataTable` (Sessions stays purpose-built) |
| Settings rebuild `view=all` rebuilds all `VIEW_ORDER` views |
| `optimize --llm/--apply` rejected; docs match |
| Compare + analytics hot paths capped with truncation notes |
| Storage upgrade confirm + circuit status summary in Resource Center |

**Deferred P2 experimental / research:** EXP-I…N, RES-P…R (flagged prototypes only).

**Release risk:** Version still 1.1.1; README/docs describe 1.2 capabilities in-tree without a
published 1.2.0 artifact until the maintainer bumps and tags.

---

## 17. Exact maintainer-only GitHub settings actions

Do **not** change these from automation. Checklist (unchecked until live verify):

[`docs/maintainers/github-settings.md`](../maintainers/github-settings.md)

Critical path for 1.2.0 publish:

1. Protect `main` (PR + required checks + no force-push).
2. Pages → GitHub Actions; dispatch Demo Pages; set homepage to
   `https://harsh-daga.github.io/Cairn/`.
3. Confirm `pypi` environment + Trusted Publisher; no long-lived PyPI token.
4. Enable Dependabot security updates / CodeQL / Scorecard upload as intended.
5. Create issue-form labels; private vulnerability reporting.
6. Bump all version sources to `1.2.0`, changelog + compare links, then annotated tag only.

---

## 18. Generated or intentionally large files

| Path | Role |
|------|------|
| `ui/src/lib/generated/api-types.ts`, `filter-grammar.ts` | OpenAPI / filter grammar |
| `docs/api/openapi-compat.json`, `generated.md`, `filter-grammar.json` | API drift |
| `docs/cli.md` | Generated CLI reference |
| `docs/configuration-reference.md` | Generated config keys |
| `ACCURACY.md`, `server/ingest/accuracy_data.json` | Accuracy tables |
| `server/static/**`, `server/static_file/**` | Built UI bundles |
| `docs/plans/v1.2.0-*-results.json` | Perf/coverage baselines |
| `docs/assets/**` | Screenshots / hero media |
| `tests/fixtures/db/pre-1.2.sqlite` | Migration fixture |

---

## 19. Confirmation: no tag, publish, push, or remote configuration

During this implementation continuation:

- **No** `git tag` for 1.2.0  
- **No** `git push` / force-push  
- **No** PyPI publish / GitHub Release creation  
- **No** changes to GitHub repository settings, environments, or Trusted Publisher via API  
- Working tree remains local modifications on `main` tracking `origin/main` at 1.1.1  

Release authority stays with the maintainer after version bump + annotated tag.

---

## Appendix — how to re-verify locally

```bash
uv run python scripts/release_check.py
```

Acceptance reports under `docs/plans/v1.2.0-acceptance-*.md`.
