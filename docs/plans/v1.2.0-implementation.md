# Cairn 1.2.0 implementation tracker

Canonical requirements: [`v1.2.0-revamp-prompt.md`](v1.2.0-revamp-prompt.md)  
Audit: [`v1.2.0-audit.md`](v1.2.0-audit.md)

Status values: `pending`, `in progress`, `partial`, `complete`, `blocked`, `deferred`.
`Complete` requires behavior tests and evidence. `Partial` means the shipped slice is real but
named scope is intentionally unfinished. Experimental work must be explicitly flagged and off by
default when it adds overhead. Research work must remain an honest prototype/document, not a
product claim.

| ID | Priority | Area | Deliverable | Existing asset | Decision | Tests | Docs | Status |
|---|---|---|---|---|---|---|---|---|
| AUD-01 | P0 | Audit | Required inventories, baseline, screenshots, timings, portfolio | 1.1.1 repo | extend | baseline commands | audit | complete |
| ADR-01 | P0 | Architecture | Time range/timezone semantics | `days` presets | extend | contract tests | ADR | complete |
| ADR-02 | P0 | Architecture | Static-export query parity | static exporter | replace | file/browser parity | ADR | complete |
| ADR-03 | P0 | Architecture | OpenAPI and generated TS types | handwritten types | replace | drift/compat | ADR | complete |
| ADR-04 | P1 | Architecture | Detector consolidation | detector registry | merge | fixtures/properties | ADR | complete |
| ADR-05 | P1 | Design | Theme and accessible tokens | dark theme | replace | contrast/theme | ADR | complete |
| ADR-06 | P1 | Performance | Pagination/virtualization | trace pagination | extend | large fixture | ADR | complete |
| ADR-07 | P1 | Guard | Attribution and instruction events | experiments/git | extend | migration/association | ADR | complete |
| ADR-08 | P0 | Release | Build/publish/provenance | main-push publish | replace | workflow invariants | ADR | complete |
| ADR-09 | P0 | Security | Untrusted telemetry, LLM, MCP boundaries | reflector/stdio MCP | extend | hostile inputs | ADR | complete |
| ADR-10 | P0 | Portability | Evidence/archive schema compatibility | trace bundles | new | round trip/hostile | ADR | complete |
| ADR-11 | P0 | Resources | Circuit breakers, reference storage, egress | watcher/raw spans | new | saturation/drift | ADR | complete |
| T00-01 | P0 | Docs | Audit every public claim/link/command/route/tool/action/version | partial docs tests | extend | public-claim suite | all public docs | complete |
| T00-02 | P0 | Security | Accurate SECURITY policy and threat model | stale SECURITY | replace | claim/security tests | security docs | complete |
| T00-03 | P0 | Community | Governance, maintainers, citation, owners, issue forms, settings checklist | partial files | new/extend | docs/YAML | maintainer docs | complete |
| T00-04 | P0 | Packaging | SPDX metadata, URLs, contents/licenses/notices, installers/Homebrew | pyproject/installers | extend | build/install/content | packaging docs | complete |
| T00-05 | P0 | API | Bounds, standard errors, operation IDs, typed response contracts | FastAPI routers | extend | API/property/compat | generated API | complete |
| T00-06 | P0 | Static/privacy | Scrub static payloads, remove absolute paths, explicit unsupported states | static exporter | replace | secrets/path/file UI | export docs | complete |
| T00-07 | P0 | HTTP security | Auth for HTTP/SSE, Host/Origin/CORS/token redirect/security headers | auth middleware | extend | matrix tests | threat model | complete |
| T00-08 | P0 | Local privacy | Restrictive permissions and doctor repair | workspace paths | new | umask/platform | privacy docs | complete |
| T00-09 | P0 | Network | Default no-network test and explicit provider preview/consent | reflector | extend | socket denial/egress | privacy docs | complete |
| T00-10 | P0 | Migration | Real pre-1.2 fixture, backup, idempotency, interruption/corruption | numbered migrations | extend | migration matrix | data model | complete |
| T00-11 | P0 | Ingest privacy | Prevent or explicitly classify cross-project global-history ingestion | global adapters | replace boundary | adapter/privacy fixtures | adapters/privacy | complete |
| T01-01 | P0 | CI | Frozen deterministic Python 3.11/3.12/3.13 gates; pinned actions/permissions | CI workflow | replace | workflow tests | CI docs | complete |
| T01-02 | P0 | UI CI | ESLint, TS, Vitest, format, generated drift, bundle gate | package scripts | new/extend | CI | contributing | complete |
| T01-03 | P0 | Browser CI | Mandatory seeded Playwright; static `file://`; no silent skip | skipping suite | replace | Playwright | CI docs | complete |
| T01-04 | P1 | Coverage | Python/UI branch baseline and changed-line ratchet | none | new | reports | testing docs | complete |
| T01-05 | P0 | Security automation | CodeQL, Scorecard, dependency review/scanner, suppression policy | Dependabot | new | workflow validation | OSPS report | complete |
| T01-06 | P0 | Release | Tag/manual build-once, artifact test, Trusted Publishing, checksums/SBOM/provenance | publish workflow | replace | workflow/release check | release docs | complete |
| T01-07 | P0 | Reproducibility | Normalized wheel/sdist/static rebuild assessment | release_check | extend | two-build compare | report | complete |
| T02-01 | P1 | Backend | Decompose payloads/schemas by domain, preserve imports | oversized modules | refactor | all API tests | architecture | complete |
| T02-02 | P1 | CLI | Decompose Typer commands, preserve `server.cli:app` and surface | 615-line CLI | refactor | CLI compat | generated CLI | complete |
| T02-03 | P1 | Demo/adapters | Split seed scenarios and parsing stages where cohesive | oversized modules | refactor | determinism/conformance | contributor map | complete |
| T02-04 | P0 | OpenAPI | Generate committed TS types/API docs and compatibility snapshot | partial generator | replace | drift/snapshot | generated API | complete |
| T02-05 | P0 | Time | Presets/custom UTC `[start,end)`, timezone, prior period, legacy `days` | `days` only | extend | cross-layer | config/API | complete |
| T02-06 | P1 | Data | Index/query review, bounded pagination/streaming, integrity diagnostics | SQLite repos | extend | query plan/doctor | data model | complete |
| T02-07 | P1 | Performance | Deterministic small/medium/10k fixtures and budgets | 120-trace demo | new | benchmarks | performance | complete |
| T02-08 | P0 | Configuration | Typed precedence, atomic get/set/unset/list, redaction, generated reference | split config systems | replace | runtime/parity | configuration | complete |
| T03-01 | P1 | Theme | Dark/light/system semantic tokens and pre-mount bootstrap | dark theme | replace | UI/E2E | accessibility | complete |
| T03-02 | P1 | Accessibility | Axe/E2E keyboard/reflow done; manual AT receipt open | partial semantics | extend | axe/E2E/manual | checklist | partial |
| T03-03 | P1 | Primitives | Accessible card/stat/table/overlays/range/error/copy primitives | common components | consolidate | component tests | UI architecture | complete |
| T03-04 | P1 | Charts | Shared accessible chart frame/formatters/tables | visx components | consolidate | component/a11y | UI architecture | complete |
| T03-05 | P1 | Shell | IA, rail/mobile nav, breadcrumbs, palette, shortcuts, offline states | current shell | replace | keyboard/E2E | UI tour | complete |
| T03-06 | P1 | First run | Local/privacy explanation, adapter states, sync/demo/setup journeys | generic empties | replace | clean-home E2E | getting started | complete |
| T03-07 | P1 | Responsive | Browser policy, 320px/200%, touch/reduced motion/high contrast | Chromium only | extend | browser matrix | accessibility | complete |
| T04-01 | P1 | Overview | Answer hero, four shields, causes/evidence, stats, trend, attention, recap | Overview | extend | API/UI/E2E | UI tour | complete |
| T04-02 | P1 | Sessions/Search | Shared typed grammar, bounded server pagination, URLs, saved views, keyboard | pages/search SQL | merge/extend | property/contract/UI | UI tour | complete |
| T04-03 | P1 | Session detail | Waterfall/replay/transcript/receipt/corrections/postmortem/shields/live tail | detail page | extend | large/partial/a11y | verification/UI | complete |
| T04-04 | P1 | Session diff | Comparability, signed deltas, regions/outcomes/diagnose/evidence | basic diff | extend | API/UI/static | UI tour | complete |
| T04-05 | P1 | Context | Region/cache/rebilling/coverage/agent comparison/evidence | Context | extend | API/UI/static | UI tour | complete |
| T04-06 | P1 | Tools | Normalized tool analytics, coverage and evidence | detectors/tool spans | new | analyzer/API/UI | UI tour | complete |
| T04-07 | P1 | Files | Reads/edits/reverts/churn/cost/evidence, safe paths | file summaries | extend | analyzer/API/UI | UI tour | complete |
| T04-08 | P1 | Agents | Trends, fingerprint, handoff table, health/model/sample sizes | Agents | extend | API/UI | UI tour | complete |
| T04-09 | P1 | Compare | Difficulty-aware outcome/cost/debt/correction comparison with intervals | difficulty/agents | new | stats/API/UI | UI tour | complete |
| T04-10 | P1 | Behavior | Verdict/baseline/drift/control/evidence event context | Behavior | extend | API/UI | UI tour | complete |
| T04-11 | P1 | Quality | Outcome vs process, debt/claims/calibration/labels | Quality | extend | API/UI | UI tour | complete |
| T04-12 | P1 | Insights | Prioritization, dedupe, evidence panel, snooze migration/actions | lifecycle board | extend | migration/action/UI | optimize | complete |
| T04-13 | P1 | Optimize | Portfolio/decay/revert/source/Guard links | experiment board | extend | stats/action/UI | optimize | complete |
| T04-14 | P1 | Live | Heartbeat/reconnect/ticks/checkpoints/backpressure/a11y | SSE page | extend | SSE/UI/E2E | UI tour | complete |
| T04-15 | P1 | Settings | Workspace/appearance/budget/adapters/data/MCP/quality/privacy/about | Settings | replace | action/UI/E2E | configuration | complete |
| T04-16 | P1 | Guard | Git instruction events, association intervals/confounds, API/static/CLI | experiments/git | new | migration/stats/UI | guard | complete |
| T04-17 | P1 | Recap | Deterministic weekly route/Guard/action/share/scheduling | recap API/CLI | extend | boundary/UI/static | recap docs | complete |
| T05-01 | P1 | Budget | Monthly/daily/weekly burn/projections/timezone/action parity | budgets config | extend | config/API/CLI/UI | config | complete |
| T05-02 | P1 | Detectors | Consolidated retry/context/model/schema families and contracts | detector registry | merge/extend | fixtures/properties | optimize | complete |
| T05-03 | P1 | Postmortem | Deterministic diagnose-based postmortems and Markdown | diagnose cascade | new | fixtures/API/static | verification | complete |
| T05-04 | P1 | Optimize | Portfolio/decay opportunistic evaluation and history | experiments | extend | stats/migration | optimize | complete |
| T05-05 | P1 | SSE | Coalesced bounded `session_cost_tick` events | EventBus | extend | clock/burst/reconnect | API | complete |
| T05-06 | P1 | MCP | `cairn_context_budget`, bounded/fresh/read-only | MCP tools | new | schema/partial | MCP | complete |
| T05-07 | P1 | CLI | stats/top/why/guard stable JSON and exit codes | current CLI | extend | CLI | CLI docs | complete |
| T05-08 | P0 | Session export | Self-contained deterministic scrubbed CSP HTML | bundle/static export | new | hostile/remote/path | privacy | complete |
| T05-09 | P1 | Verification | Intent, claims, evidence, timeline, debt, receipt v1/rebuild | outcomes/evidence | new | fixtures/properties/API | verification | complete |
| T05-10 | P1 | Regression | Versioned create/run/compare/list/import/export (record-from-trace only) | none | new | round trip/hostile | regressions | complete |
| T05-11 | P1 | Supervision | Corrections classifier/relabel/recovery and handoff capsule | messages/spans | new | false positives/budget | verification | complete |
| T05-12 | P1 | Policy/review | Typed advisory policies, review risk, `cairn check` integration | check/config | new | evaluator/action | policy docs | complete |
| T05-13 | P1 | MCP evidence | verification/handoff/policy/regression/next-evidence tools | MCP | new | schemas/privacy | MCP | complete |
| T05-14 | P1 | CLI evidence | receipt/regression/handoff/review/verify/archive/resource/privacy | CLI | new | schemas/exits | CLI docs | complete |
| T05-15 | P1 | Evidence closure | Repository-grounded next check preview, cost/limits/approval/feedback | CI/test history | new | hostile suggestions | verification | complete |
| T06-01 | P0 | Process | Manual/Efficient/Live collection modes and truthful controls/shutdown | auto pipeline | replace | mode/soak | resource shield | complete |
| T06-02 | P0 | Resources | Resource contracts, diagnostics, disk inventory/forecast | doctor/workspace | new | process/bench | performance | complete |
| T06-03 | P0 | Watcher | Single coalesced event path, adaptive discovery, stale watch removal | polling watcher | replace | burst/history | architecture | complete |
| T06-04 | P0 | Jobs | Bounded executor, dedupe, cancellation, progress, expiry/backpressure | thread/job | replace | saturation/shutdown | architecture | complete |
| T06-05 | P0 | Storage | Metrics/Balanced/Forensic modes, strip/truncate/scrub | raw text | new | migration/resume | privacy | complete |
| T06-06 | P1 | Dedup/search | Content-addressed/reference storage assessment and consistent FTS | raw/FTS | new/replace | collision/bench/rebuild | ADR/performance | complete |
| T06-07 | P0 | Git privacy | Private paths/modes, `.git/info/exclude` approval, tracked-data detection | doctor | extend | worktrees/symlinks | privacy | complete |
| T06-08 | P0 | Lifecycle | Retention, dry-run cleanup, backup/restore, compact/delete/integrity | none/partial | new | destructive safety | resource shield | complete |
| T06-09 | P1 | Resource UI/CLI | Resource & Privacy Center with backup list/dry-run/typed restore | Settings/doctor | new | action/UI/CLI | resource shield | complete |
| T06-10 | P1 | Resource tests | Accelerated soak v2 (idle / EventBus / jobs windows; not 8h) | perf tests | new | scheduled/stable PR | performance | complete |
| T06-11 | P0 | Pricing | Bundled source/version/effective date, local override, stale/unknown | price JSON | extend | offline | accuracy | complete |
| T06-12 | P0 | Archive | Lossless bounded versioned Cairn archive plus OTLP-loss contract | trace bundle | new | round trip/bombs | portability | complete |
| T06-13 | P0 | Egress | Privacy-minimized egress ledger and previews | reflector | new | no-network/ledger | privacy | complete |
| T06-14 | P0 | Circuit breakers | Input/parse/memory/queue/disk budgets, quarantine/retry | adapter errors | new | hostile matrix | resource shield | complete |
| T06-15 | P1 | Reference mode | Source-authoritative references and drift detection | ingest cursors | new | source move/rewrite | privacy | complete |
| T07-01 | P1 | README | Concise evidence-led 1.2 README with real media/limits | 1.1 README | replace | render/claims | README | complete |
| T07-02 | P1 | Documentation | Required product/architecture/security/performance/accessibility docs | partial docs | new/extend | links/claims | docs set | complete |
| T07-03 | P1 | Pages | Canonical landing/demo, base path, SPA refresh, artifact preview | demo-pages | replace | project path/file | maintainer docs | complete |
| T07-04 | P1 | Examples | Deterministic e2e plus CI/OTLP/MCP/adapter/export workflows | e2e demo | extend | CI examples | examples | complete |
| T07-05 | P1 | Contributing | Bootstrap/map/matrix/generated/deps/screens/migrations/API/a11y/privacy | short guide | replace | docs checks | contributing | complete |
| EXP-I | P2 | Experimental | Intent Contract and requirement coverage, flagged | none | new | accuracy/UX | hypothesis card | deferred |
| EXP-J | P2 | Experimental | Repository agent-readiness/friction map, flagged | file/context data | new | false positives | hypothesis card | deferred |
| EXP-K | P2 | Experimental | Instruction Intelligence graph, propose-only | instruction targets | new | precedence/conflict | hypothesis card | deferred |
| EXP-L | P2 | Experimental | Advisory autonomy policies and MCP consultation | check/config | new | advisory/enforced labels | hypothesis card | deferred |
| EXP-M | P2 | Experimental | Stop/split/handoff coach and local feedback | stop tool | extend | false positives | hypothesis card | deferred |
| EXP-N | P2 | Experimental | Privacy-preserving team evidence packs, export only | rule-effect export | new | thresholds/privacy | hypothesis card | deferred |
| RES-P | P2 | Research | Coding-agent trace profile and prototype export | OTLP | extend | unknown fields | standard draft | deferred |
| RES-Q | P2 | Research | Matched counterfactual explorer prototype with warnings | difficulty/stats | new | sensitivity | research note | deferred |
| RES-R | P2 | Research | Unsigned agent-change provenance manifest | receipt hashes | new | deterministic/privacy | schema docs | deferred |
| T08-01 | P0 | Acceptance | Python analyzer/API/action/CLI/MCP/migration/security/hostile tests | pytest | extend | full suite | report | complete |
| T08-02 | P0 | Acceptance | UI unit/component/accessibility tests | Vitest | extend | full suite | report | complete |
| T08-03 | P0 | Acceptance | Mandatory multi-browser user-journey matrix and screenshots | Playwright | replace | browser suite | report | complete |
| T08-04 | P1 | Acceptance | Stable scale/resource/archive/browser performance matrix | limited perf | extend | benchmarks | report | complete |
| FIN-01 | P0 | Release gate | Single complete local release command; all generated files current | release_check | replace | all gates | release docs | complete |
| FIN-02 | P0 | Final report | Required 19-part evidence report and maintainer-only actions | none | new | evidence links | final report | complete |

## Execution log

### 2026-07-17 — Audit and baseline

- Read the complete canonical specification and all required root/docs Markdown, workflows,
  `pyproject.toml`, `ui/package.json`, installer, release, Playwright, Vite, TypeScript, Tailwind,
  pre-commit, and ignore configuration.
- Baseline results and measurements are recorded in the audit.
- Captured deterministic desktop/tablet/mobile screenshots under `docs/assets/v1.2/baseline/`.
- Added and accepted ADR 0001–0011 under `docs/architecture/decisions/`.
- Added bounded public query parameters and standardized public API error envelopes. Focused
  API/static/OTLP/scrub verification: `49 passed`; Ruff and strict mypy passed.
- Scrubbed credentials and absolute roots from static exports, added protected-output and symlink
  refusal, private output modes, recap/manifest parity, and matching static query bounds.
- Prevented global-history adapters from claiming unrelated projects unless a bounded structured
  record contains an explicit matching project path. Adapter/privacy verification: `29 passed`;
  Ruff and strict mypy passed.
- Restricted SQLite, WAL, SHM, and export artifacts under permissive umasks. Store/static
  verification: `15 passed`; Ruff and strict mypy passed.
- Replaced the stale security policy and added `docs/security/threat-model.md`, including honest
  non-goals and untrusted-telemetry/provider/MCP boundaries.
- Enforced Host and same-origin browser boundaries, explicit preflight behavior, token coverage
  for static/API/SSE/OTLP, token-free no-store redirects, and browser security headers. The static
  export now has a local external bootstrap and file-compatible CSP. Focused verification:
  `49 passed`, followed by `32 passed`; Ruff and strict mypy passed.
- Added private atomic/append helpers across database, runtime state, exports, MCP sidecars/config,
  and managed-block backups. `cairn doctor` now detects permissive modes and
  `--repair-permissions` repairs them explicitly. Sensitive-write verification: `36 passed` and
  `54 passed`; Ruff and strict mypy passed.
- Added a socket-denial regression for default health/demo/static-export journeys. Optional LLM
  execution now defaults denied and requires a no-network preview whose token binds the exact
  destination, backend, model, and content; CLI and API/action surfaces are in parity. Reflector,
  API, no-network, and action-parity verification: `29 passed`; Ruff and strict mypy passed.
- Replaced generated FastAPI IDs with a concise stable operation-ID convention and committed
  deterministic OpenAPI-derived TypeScript, compatibility, and API-index artifacts with a drift
  gate. Replaced handwritten UI transport models with generated imports, introduced named nested
  response DTOs, and documented the shared error envelope and example for every public operation.
  API/static/parity/provider verification: `66 passed`; strict mypy and Ruff passed; all `43` UI
  tests, UI typecheck, and generated drift checks passed without first-party warnings.
- Declared static snapshot capabilities and unsupported features in its manifest, surfaced capture
  and limitations in the shell, removed actions from the static command palette, and made
  uncaptured queries/pagination fail with a specific explanation. Static verification: `4 passed`;
  UI typecheck and four focused Vitest cases passed.
- Made each numbered migration transactional without `executescript` implicit commits, added
  verified owner-only pre-migration backups for existing user schemas, and added doctor
  `quick_check` corruption diagnostics. A fixed 1.1.1 logical SQLite fixture proves trace/span data
  survives upgrade and is present in the backup. Migration/doctor verification: `21 passed`, plus
  three focused fixture/interruption/corruption cases; Ruff and strict mypy passed.
- Added a strict ESLint flat configuration with TypeScript, React Hooks, and Fast Refresh rules;
  moved shared logic out of component modules and resolved every reported hook dependency rather
  than suppressing findings. UI lint, typecheck, and all `43` Vitest cases pass.
- Restored pytest's default hidden-directory exclusion so Hypothesis collection is warning-free.
  Replaced Playwright's server/data skips with an isolated deterministic seed server and hard
  prerequisite assertions: all `19` Chromium journeys pass with zero skips.
- Rebuilt CI around frozen uv/npm locks, Python 3.11/3.12/3.13, pinned full-SHA actions,
  least-privilege workflow permissions, generated drift, formatting, failure artifacts, and a
  deterministic `350 KiB` JavaScript gzip budget. Workflow invariant tests and YAML parsing pass;
  the current bundle is `172,554` gzip bytes.
- Made static snapshots genuinely double-clickable: exported payloads are embedded in a local
  external data script, the reviewed IIFE is loaded as a deferred classic script, bundled assets
  are file-relative, and the UI selects hash routing in snapshot mode. The complete Playwright
  suite now runs `20 passed` with zero skips, including `file://` navigation, captured session data,
  and an empty console/page-error assertion.
- Added pinned Python and JavaScript/TypeScript CodeQL, moderate-threshold dependency review,
  weekly/PR `pip-audit` and `npm audit`, and Scorecard SARIF/artifact automation with scoped token
  permissions. Upgraded Vitest to remove all five npm advisories; both package ecosystems now
  report no known vulnerabilities. Added the time-bounded suppression policy and a dated mapping
  of every OSPS Baseline v2026.02.19 control plus Best Practices Badge gaps without claiming
  compliance. Workflow/docs verification: `21 passed`.
- Replaced main-push publishing with an annotated-tag/manual-existing-tag build-once flow. It
  validates release identity, builds once, generates checksums/changelog notes/CycloneDX SBOM,
  attests subjects, downloads and clean-installs the exact artifacts, gates Trusted Publishing on
  the protected `pypi` environment, creates the GitHub Release from those files, and verifies the
  public package. No workflow was run and nothing was published.
- Added a normalized same-source reproducibility gate. Two static builds and two wheel/sdist builds
  produced byte-identical outputs (`26` static files and the complete archive set). The report
  limits the claim to same-platform evidence and documents checksum/attestation verification.
- Restricted the sdist to an explicit public source allowlist after inspection found local
  `AGENTS.md`/Hypothesis state in the old archive. Added SPDX metadata, complete project URLs,
  bundled-font OFL text, third-party notices, and fail-closed UI packaging. The expanded release
  check now verifies five version sources, changelog links, generated drift, README assets/links,
  offline UI references, workflow invariants, wheel/sdist metadata and contents, and clean-wheel
  import/CLI/doctor; it passes locally.
- Added accurate maintainer/governance/citation/ownership records, privacy-safe bug/adapter/feature/
  docs issue forms, a contributor work map, and the exact GitHub/Pages/security/PyPI settings
  checklist. External access and settings remain explicitly unverified.
- Removed remote-script pipe execution from public installers and setup docs, made uv bootstrap
  download to a temporary file with cleanup, made doctor failures visible, and documented pinned,
  mirror/proxy, offline, idempotent upgrade, and uninstall/data-retention paths. Homebrew is now
  explicitly unsupported rather than aspirational. Installer/docs/workflow verification:
  `24 passed`; the release check still passes.
- Re-ran the public-claim suite across version sources, generated CLI/API/action surfaces, internal
  links, retired terms, install URLs, and community metadata. All `24` non-placeholder HTTPS URLs
  in public top-level/docs pages returned HTTP `200`; route/action parity remains generated and
  behavior-tested.
- Implemented the shared time contract across overview, traces, agents, behavior, quality, and
  analytics: bounded presets, legacy `days`, custom IANA-timezone bounds, UTC half-open filtering,
  equal-length prior bounds, resolved-range response metadata, stable UI query keys, and URL state.
  Usage aggregation now reads exact trace bounds rather than overcounting partial rollup days.
  Static manifests publish capture/data bounds and supported queries and explicitly reject custom
  ranges. Cross-layer verification: `47 passed` backend focused tests, strict Ruff/mypy, all `46`
  UI tests, UI typecheck/lint/format, all `21` Playwright journeys with zero skips (including
  custom URL/API bounds and static rejection), plus generated OpenAPI/type drift regeneration.
- Replaced split `Settings`/`UserConfig`/environment/action behavior with layered typed
  configuration: explicit CLI > environment > workspace TOML > user TOML > defaults. Added
  atomic owner-only comment-preserving set/unset, get/list source metadata, secret redaction,
  unknown/type errors, legacy aliases, workspace scope, and schema-generated reference docs.
  Budget gates, test commands, diagnose tunables, pricing overrides, optimize backend/holdout, and
  MCP client/auto-install now read the same model. API/action/CLI parity and configuration-focused
  verification passed within `116` focused backend tests; the complete backend suite passed
  `313`; strict Ruff/mypy and all `46` UI tests/type/lint/format gates passed.
- Added Python and UI statement/branch coverage reports, explicit generated/vendor exclusions, an
  observed non-decreasing baseline, and a `90%` changed-executable-line pull-request ratchet.
  Measured baselines are Python `81.62%` statements/`58.78%` branches and UI `28.89%`
  statements/`20.64%` branches; no higher vanity threshold was invented. CI publishes JSON, XML,
  LCOV, and UI report artifacts. Coverage/workflow/docs checks passed (`30` focused tests), the
  local ratchet passed, frozen locks resolved, Ruff/format/diff checks passed, and the expanded
  release check passed.
- Decomposed the oversized public schemas and payload builders into overview, trace, analytics,
  improvement, and system domains while preserving `server.api.schemas` and
  `server.api.payloads` as identity-preserving compatibility facades. Shared time/data-note
  calculations now have one dependency-safe home, and the architecture ownership/change procedure
  is documented. Facade identity and completeness tests, generated OpenAPI drift, strict Ruff and
  mypy, `87` focused API/static/docs tests, and the complete backend suite (`318 passed`) all pass.
- Reduced `server.cli` from `724` lines to the stable app/runtime/action facade and moved callbacks
  into operational, improvement, and integration/configuration command modules. Both the installed
  `server.cli:app` entry point and `python -m server.cli` register the same app; command/group
  names, action coverage, important aliases, render-helper imports, help, generated CLI docs, and
  monkeypatch behavior are pinned by compatibility tests. Focused CLI journeys passed `46`; strict
  Ruff/mypy, the complete backend suite (`322 passed`), diff checks, and the expanded release check
  all pass.
- Split deterministic demo values into pure clock-injectable scenarios, ledger fixtures, and the
  evidence-to-verdict improvement fixture while retaining `server.demo.seed` compatibility.
  Separated Cursor's read-only `state.vscdb` discovery/decoding from streamed untrusted JSONL
  transcript normalization behind its existing facade; other adapters already retain cohesive
  parser states behind thin discovery wrappers. Read-only byte preservation, import identity,
  canonical special journeys, reseed determinism, conformance, no-network, and contributor
  boundaries are behavior-tested. Focused checks passed `39`; strict Ruff/mypy, the complete
  backend suite (`325 passed`), diff checks, and the expanded release check all pass.
- Added an append-only migration with evidence-backed composite/partial indexes for workspace
  source/project/actor time filters, agent membership, waste, reverse links, and recent improvement
  rows; query-plan tests pin the high-frequency indexes. Retired the unmaintained `spans_fts`
  duplicate of sensitive text and made canonical search exact-count but page-bounded. Repository
  calls independently enforce page/offset ceilings, rule-effect export walks bounded pages, static
  export enumerates rows in chunks and atomically streams its aggregate data file, and doctor adds
  read-only foreign-key diagnostics. ADR 0012 and the data-model guide document the tradeoffs.
  Focused data/API/static/doctor checks passed `89`; strict Ruff/mypy, the complete backend suite
  (`329 passed`), diff checks, migration packaging, clean-wheel doctor, and the release check pass.
- Added generated-on-demand deterministic 100/1,000/10,000-trace datasets (up to 52,495 spans and
  a 2,500-span trace), repeatable backend/ingest/static/browser benchmark commands, checked-in raw
  measurement reports, stable structural CI budgets, and a scheduled/manual trend artifact.
  Scale testing found and fixed cumulative replay amplification, unbounded static trace-detail
  capture, an unconstrained waterfall viewport, and an oversized eager route bundle. Replay now
  stays below the 4 MiB payload budget, static snapshots explicitly cap and declare 1,000 captured
  details while preserving total counts, the waterfall mounts 34 visible rows in the measured
  2,500-span journey, and the server build is 28 lazy chunks with a `79,797`-byte gzip entry and
  `188,133` bytes total. The dual server/file-compatible UI builds are both required in wheel and
  sdist validation. Focused performance/static/workflow checks passed `18`; strict Ruff/format/
  mypy/diff checks, all `336` backend tests, all `47` UI tests plus lint/type/generated drift, all
  `21` browser journeys, both bundle budgets, and the expanded clean-wheel release check pass.
- Rebuilt the color system around semantic RGB-channel tokens for surfaces, text, borders, focus,
  selection, overlays, seven chart series, success/severity, confidence, and estimates while
  retaining the old geological names only as compatibility aliases. Added light/dark/system
  preference, live OS-theme following, versioned migration of legacy browser state, a Settings
  control, forced-colors/reduced-motion behavior, and a synchronous same-origin pre-mount
  bootstrap that remains compatible with the strict server and `file://` CSP. Both UI build
  variants and release archives now require the bootstrap. Automated contrast calculations pin
  primary/muted/disabled/accent/severity/focus thresholds in both palettes and forbid component
  hex colors. All `56` UI tests, typecheck/lint/format, all `23` browser journeys, all `336`
  backend tests, strict Ruff/format/mypy/diff checks, dual builds/bundle budgets, static export,
  and clean-wheel release validation pass.
- Began the WCAG 2.2 AA journey gate with component-level axe and a zero-violation Playwright axe
  matrix across all 13 routes in dark and light. Added first-focus skip navigation; complete
  accessible names in collapsed/mobile navigation; every route in the bounded mobile dock; named
  meters; keyboard-focusable overflow regions; session table captions, selected-state/live
  announcements, and `j`/`k` focus movement; `j`/`k`/arrow waterfall traversal; and a shared
  modal focus trap/restore/Escape contract for the command palette, shortcut overlay, and custom
  range. Automated 320 CSS-pixel plus 200% zoom reflow passes. The initial axe run found unnamed
  meters and inaccessible scroll regions; both were fixed without suppressions. All `57` UI tests
  pass; the expanded 29-test accessibility file passed 28 journeys, then the one Context
  scroll-focus failure was fixed and passed focused rerun. Remaining before terminal status:
  chart text/table alternatives and an honestly recorded manual screen-reader smoke run.
- Added the shared `components/ui` layer with production-integrated Card/MetricHelp/Stat,
  EmptyState/Skeleton/InlineError, semantic Badge/EstimateBadge/ConfidenceInterval, CopyButton,
  Breadcrumbs, ErrorBoundary, SegmentedControl/LabeledField, Tooltip/Popover, and reusable
  Dialog/SidePanel primitives. The app router now has a local-only error boundary, existing common
  empty/error states delegate to the new semantics, and Settings uses the explicit copy primitive.
  Dialogs and panels reuse the tested trap/restore/Escape contract; side-panel width is also
  keyboard adjustable. Focused primitive tests (`5`) including axe, strict TypeScript, lint, and
  format pass. Remaining for T03-03: the server-paginated/virtual DataTable and shared
  TimeRangePicker, broader production migration, and UI architecture documentation.
- Completed the primitive layer with a semantic sortable DataTable, `aria-sort`, numeric
  alignment, `j`/`k`/arrow/Enter row behavior, bounded server pagination, and a tested 150-row
  virtual window that preserves native table/caption/header semantics. Added the shared
  TimeRangePicker and migrated the real topbar to it without changing explicit UTC half-open
  query behavior. The UI architecture guide records ownership and non-negotiable contracts.
  All `65` UI tests, strict TypeScript/lint/format, the custom-range browser journey, the
  all-route/both-theme zero-violation axe matrix (`29 passed`), dual builds, and the updated
  `81,108`-byte entry/`190,228`-byte total gzip bundle budgets pass.
- Began chart consolidation by moving the legacy card wrapper to a typed shared ChartFrame with a
  required conclusion sentence by default and optional native data-table alternative. Added the
  seven-series visibility cap and deterministic `other` aggregation helper. Existing imports
  remain compatible while pages migrate to explicit summaries/tables. Focused chart tests (`9`),
  strict TypeScript, lint, and format pass. Remaining for T03-04: migrate each chart/page to the
  shared accessible contract, keyboard inspection/crosshair behavior, centralized public
  formatting, and all-route axe/browser revalidation.
- Migrated the Behavior radar/control/EWMA and Quality funnel/histogram/cost-per-success frames to
  explicit conclusion sentences plus equivalent native data tables. The SVG remains a visual aid;
  axes, units, estimates, and values are present in text. Focused chart/behavior checks now pass
  `10` with strict TypeScript and lint. Context/Overview/Optimize and keyboard point inspection
  remain in the active T03-04 slice.
- Completed the shared chart contract across Context, Overview, Behavior, Quality, and Optimize.
  `ChartFrame` now requires an explicit conclusion at compile time and exposes native data tables;
  decorative sparklines repeat adjacent text metrics. Stacked areas support pointer plus
  Left/Right/Home/End keyboard inspection with polite announcements, apply the seven-series cap in
  the renderer with deterministic `other` aggregation, and use per-instance SVG definition IDs.
  Public chart labels use centralized number/currency/percentage/date formatting. The UI
  architecture guide records these boundaries. Strict TypeScript/lint/format, all `68` UI tests,
  the full both-theme/all-route axe and browser matrix (`52 passed`), static file navigation, both
  production builds, `git diff --check`, and bundle budgets pass: `81,357`-byte entry and
  `192,411`-byte total gzip across 28 chunks.
- During the chart browser gate, the new accessible “Spend velocity” labels exposed an ambiguous
  custom-range test locator and the static snapshot still rendered an unusable disabled Custom
  button. The journey now targets the three exact textboxes, and static mode omits the unavailable
  control while retaining the existing explanatory limitation. Both focused regressions and the
  complete `52`-journey browser gate pass.
- Completed the application shell around a single typed route registry shared by the desktop rail,
  mobile dock, and command palette. Current working routes are grouped as Monitor, Analyze, Act,
  and Utilities; Session Diff is presented as the current Compare destination. Future Tools,
  Files, Guard, and Recap rows will add their routes to this registry only when their production
  pages exist, avoiding dead navigation and prohibited visual-only shells. The rail retains active
  state and accessible names when collapsed; mobile exposes every working route.
- Added semantic breadcrumbs for Session Detail and Compare, `/` unified search, `g` then
  `o/s/l/c/i`, conflict exclusion for inputs/textareas/editable content, and an updated shortcut
  overlay. The palette now covers every working route, bounded fuzzy session jump, theme and
  preset-range commands, and the typed action manifest while withholding server-backed search and
  mutations in static mode. Workspace request failures remain visibly announced as
  `Local server disconnected`; sync/watch and adapter-quality state retain their existing
  boundaries. The UI tour documents the actual working shell without advertising future routes.
- Shell verification passes: strict TypeScript/lint/format, all `72` UI tests, all-route/both-theme
  axe coverage, keyboard shortcut/theme/active-route/breadcrumb/disconnection journeys, static
  file navigation, and the full `54`-journey Playwright suite. Dual production builds,
  `git diff --check`, and bundle budgets pass at `82,083`-byte entry and `193,370`-byte total gzip
  across 28 chunks. Browser testing exposed an initialization race in the new chord journey; the
  test now waits for the Overview route to hydrate before sending physical keys, and printable
  shortcuts are normalized without relaxing modifier or typing-target exclusions.
- Completed the first-run product flow on Overview using the real workspace health contract. It
  distinguishes no discovered logs, discovered streams ready to sync, attempted-but-unparsed
  streams, a queued sync/scan, action failure, and “workspace has sessions but none in this
  selected range.” It shows discovered adapters and parse coverage, exact local ledger location,
  local/account-free/zero-telemetry/loopback boundaries, what the first useful session unlocks,
  and real Sync/Scan/Settings/doctor next actions. The setup checklist explicitly says discovery
  never edits agent or MCP configuration.
- `Load deterministic demo` invokes the existing typed action and reports the exact launch command
  for the separate demo workspace, preventing synthetic/real mixing. An active `.cairn-demo`
  Overview now explains how to stop it and reopen the real repository. The CLI sync command and
  bare `cairn` flow use a tested shared renderer for matching no-log, unchanged, and imported next
  actions; no unsupported parse diagnosis is inferred merely from a zero-import sync. Getting
  Started documents empty states, storage, demo isolation, and the path back.
- First-run verification passes: a real temporary clean workspace with no developer cache returns
  zero traces/adapters and creates its private ledger; focused CLI/API tests (`8 passed`); all `75`
  UI tests; clean-home, selected-range expansion, and demo-return browser journeys; the complete
  both-theme/all-route browser and axe gate (`57 passed`); strict Python/UI static checks; dual
  builds; `git diff --check`; and budgets at `82,089`-byte entry and `195,588`-byte total gzip
  across 28 chunks.
- Completed the responsive/browser policy with a checked-in support statement and one mandatory
  Playwright command: the full Chromium matrix plus tagged core Firefox/WebKit journeys. CI now
  installs all three engines, and workflow invariants prevent silent removal of an engine or the
  targeted grep. The policy explicitly distinguishes Playwright WebKit compatibility evidence
  from native Safari/VoiceOver certification and touch emulation from physical-device testing.
- Added real-browser preference/input acceptance: reduced motion reports no nonessential running
  animations; forced colors retains a system-visible focus outline at least 3 CSS pixels and
  structural card borders; the 390×844 touch context exposes all 12 working routes, enforces a
  44×44 minimum Sessions target, and taps through without hover. Existing 320-CSS-pixel/200%
  reflow and both-theme axe coverage remain mandatory. Browser/accessibility/testing/CI docs
  describe the exact matrix, privacy/network independence, and limitations.
- The complete command passes `68` journeys without skips: `60` Chromium plus four each in Firefox
  and WebKit, including Overview, custom UTC half-open ranges, Session Detail, and direct
  `file://` static navigation. Local evidence used Playwright `1.61.1`, Chromium
  `149.0.7827.55`, Firefox `151.0`, and WebKit `26.5`. Documentation/workflow invariants pass
  `25`; strict TypeScript/lint/format and `git diff --check` pass.
- Completed Overview as an evidence-backed answer surface. Its typed API now supplies measured
  quality and cost-per-success samples, equal-period deltas, a quality sparkline, current-month
  projection with a seven-active-day threshold, typed monthly-budget state, and conservative
  cause impact. Every cause has an evidence-derived low/medium confidence explanation and at most
  five exact session/span/path/tool-name evidence rows; the side panel states the allocation
  limitation and its links select the exact waterfall span through URL state.
- Added four independent Verification, Scope, Privacy, and Resource summaries. Each has its own
  state, facts, limitation, and working action; unavailable inputs remain unavailable rather than
  being folded into a trust score or mislabeled healthy. Applied-experiment chart annotations
  open the exact expanded Optimize card. Guard annotations remain dependent on T04-16 and are not
  fabricated.
- Replaced the generic Overview insight queue with eight bounded typed attention categories:
  failed outcomes, recorded verification gaps, completion claims, drift, retry storms, adapter
  parse health, budget risk, and decayed rules. Available checks have measured clear/attention
  states and exact deep links; claim support and rule decay are explicitly unavailable until
  their evidence-producing tracker rows land. The Insights and Optimize pages honor exact query
  targets.
- Added the working palette-only `/recap` route and Overview preview ahead of the richer T04-17
  scope. Dismissal stores only the current UTC Monday period key, so a new weekly period is not
  permanently hidden. The route works in HTTP and captured `file://` snapshots. Guard events,
  best/worst privacy-safe sessions, the ranked recommendation, sharing, and scheduling remain
  owned by T04-17.
- Overview verification passes strict Ruff/mypy, generated OpenAPI/type drift, all `66` focused
  API/static/docs/module tests, all `76` UI tests, TypeScript/lint/format, and `git diff --check`.
  The complete browser gate passes `73` journeys: `65` Chromium plus four each Firefox and WebKit,
  including the all-route/both-theme axe matrix, exact insight/experiment/span links, weekly
  Recap, and static `file://` navigation. Dual production builds pass the explicit budgets at
  `82,395`-byte HTTP entry, `198,833` bytes across 29 HTTP chunks, and `210,691`-byte static IIFE
  gzip.
- Completed the shared Sessions/Search filter contract around a single shell-like typed parser and
  SQL evaluator. It supports quoted/escaped phrases plus agent, source, compatible `is:` status,
  numeric cost comparisons, outcome, relative file, tool, ISO `after`, and verification filters.
  Unknown, invalid, or recognized-but-not-yet-available claim/correction/risk filters return the
  same structured error and zero rows on both endpoints, preventing accidental query broadening.
  Hypothesis hostile-text coverage and cross-endpoint fixtures pin that contract.
- Generated the UI autocomplete operator manifest and machine-readable API grammar directly from
  the backend source. Parsed filters render as accessible removable chips. Existing source/agent
  parameters and local saved-view records remain compatible; stable URL-backed sorts now cover
  recent, cost, waste, duration, tokens, and quality.
- Rebuilt Sessions as a server-paginated mission-control view with virtualized visible rows. Each
  row includes title, agent/source, start/duration, token flow, cost, waste, quality, outcome,
  verification, and data-quality state. Expandable previews show the bounded first user request
  and top relative files as text only; static export fully redacts the request and scrubs paths.
  Exactly two selections retain Session Diff, while three through twenty produce a descriptive
  aggregate with an explicit non-comparability/non-causality limitation.
- Search now groups exact trace/span links, safely highlights plain text, pages bounded results,
  exposes counted agent/outcome/date/file/tool facets, supports `j`/`k` plus Enter, and keeps
  deletable recent queries only in browser storage. Because FTS is unavailable, the response/UI
  explicitly label the bounded compatibility scan. Privacy-safe copy actions exclude free text,
  paths, and agent identifiers from copied filter URLs.
- Sessions/Search verification passes strict Ruff/mypy, generated drift, `81` focused backend,
  static, module-boundary, docs, performance-fixture, and release-script tests; all `79` UI tests;
  TypeScript/lint/format; and `git diff --check`. The complete browser gate passes `74` journeys
  across Chromium, Firefox, and WebKit, including both-theme axe, virtual-row mounting,
  filter-error/chip behavior, multi-session summary, keyboard Search, counted/degraded Search,
  phone containment, and static `file://` navigation. Dual builds pass at `82,501`-byte HTTP
  entry, `203,727` bytes across 31 chunks, and `214,331`-byte static IIFE gzip. The bundle gate now
  reads the actual HTML module entry instead of assuming every `index-*` lazy chunk is an entry.
- Confirmed no commit, push, tag, publish, release, or remote-setting change occurred.

## Current wave

Wave 1 acceptance:

1. P0 public-claim/security/static/query/error/permission/project-attribution boundaries have
   behavior tests;
2. the repository remains buildable and the tracker records every completed command.

Next dependency: T04-13 Optimize portfolio/decay depth, while T03-02 retains the honestly
documented manual assistive-technology smoke-run item.

### 2026-07-18 — T04-03 Session Detail implementation checkpoint

- Added independent session Verification, Scope, Privacy, and workspace Resource shield payloads
  with facts, limitations, and actions; no aggregate trust score or unmeasured healthy state is
  invented.
- Extended the flagship detail route with outcome/quality/cost/token/duration/model/source/data
  quality metadata, active-session two-second live polling, working compare/post-mortem/scrubbed
  export/copy-link actions, and stable `span`, `seq`, `mode`, `zoom`, and `tab` URL state.
- Added URL-addressable Transcript, Receipt, Corrections, and Post-mortem views. Imported content
  remains plain text, tool results are collapsed, and unavailable requirements, claim links,
  correction classification, validation ordering, and causal narratives are stated explicitly.
- Added play/pause replay at 1×/4×/16×, synchronized context summary, accessible minimap controls,
  span-kind markers, duration/token columns, and a Summary/Content/Context/Links inspector.
  Waterfall flattening is now iterative and visual indentation is bounded; 10,000 wide rows and a
  5,000-level parent chain are behavior-tested.
- The scoped export action now verifies workspace ownership and writes a private scrubbed bundle
  containing normalized spans, links, outcomes, data quality, and diagnostics plus an exact field
  class manifest. Its focused backend test passes; Ruff and strict mypy pass.
- Current focused verification: all `80` pre-existing-plus-deep-tree UI tests passed, the three new
  untrusted-content/shield/replay component tests pass, TypeScript and strict ESLint pass, the
  scoped backend tests pass (`3 passed`), and the expanded Chromium Session Detail journey passes.
  The terminal gates then passed: all `83` UI tests; `82` focused backend/static/docs/performance
  tests; all `74` Playwright journeys across Chromium, Firefox, and WebKit; generated drift;
  formatting/diff checks; strict Ruff/mypy/TypeScript/ESLint; and both production builds. Final
  bundle measurements are `82,496`-byte HTTP entry, `208,285` bytes across 31 HTTP chunks, and
  `218,932`-byte direct-file IIFE gzip.
- T04-03 is complete. The Receipt, Corrections, and Post-mortem views intentionally label evidence
  that depends on later receipt/classifier/diagnose tracker rows instead of fabricating it.
  No commit, push, tag, publish, release, or remote-setting change occurred.

Next dependency: T04-04 Session Diff comparability, signed metrics, aligned timeline, region/model/
outcome differences, diagnose links, and static parity.

### 2026-07-18 — T04-04 Session Diff implementation checkpoint

- Preserved the existing aligned-turn/summary contract and added one typed `analysis` field with
  total token and duration deltas, region composition, model sets, outcomes, diagnostics,
  comparability, deterministic change statements, and exact session/failure-span evidence.
- Comparison validity now distinguishes comparable, limited, and not-comparable states from
  recorded source/project/title/difficulty facts. Every response includes the explicit limitation
  that descriptive associations do not establish causality.
- Enforced current-workspace ownership for both arbitrary trace IDs. Large sequences switch away
  from quadratic LCS above 1,000,000 cells, align at most 2,000 spans per side, and publish the
  active mode/truncation limitation.
- Rebuilt the page with five signed metrics, outcome/model/diagnose comparison, region table,
  evidence-linked “what changed,” a side-by-side aligned mini-waterfall, and 200-row progressive
  rendering. Untrusted names remain inert React text.
- Static snapshots now retain safe internal relational IDs and capture ten adjacent recent diff
  pairs; uncaptured pairs still fail explicitly. The direct `file://` browser journey found and
  fixed the prior referential-ID scrub bug and now opens a working captured comparison.
- Current focused verification: strict Ruff/mypy and module/generated checks pass; `12` backend
  diff/static tests pass; the dynamic Chromium comparison journey passes; the direct-file Chromium
  comparison journey passes; and focused UI types/lint/tests pass. Terminal verification then
  passed all `84` UI tests, `86` focused backend/API/static/docs/performance tests, and all `75`
  Playwright journeys across Chromium, Firefox, and WebKit, plus both-theme axe, generated drift,
  strict format/type/lint/Ruff/mypy, dual production builds, bundle budgets, and `git diff --check`.
  Final gzip measurements are `82,499`-byte HTTP entry, `210,199` bytes across 31 HTTP chunks, and
  `220,601`-byte direct-file IIFE.
- T04-04 is complete. No commit, push, tag, publish, release, or remote-setting change occurred.

Next dependency: T04-05 Context region composition/trend, rebilling, cache coverage, schema
overhead, per-agent comparison, evidence, and adapter data-quality coverage.

### 2026-07-18 — T04-05 Context implementation checkpoint

- Extended `/api/analytics/regions` (no parallel API) with an answer-first `ledger` summary:
  deterministic conclusion, mapped/rebilled/schema ratios, region coverage, next action with
  optional evidence href, and an explicit limitation that mapped rows are not a partition of input
  tokens and do not prove avoidable spend. Cache savings remain unavailable without provider
  billing semantics.
- Context UI consumes the ledger, labels estimates, exposes MetricHelp, captions tables, shows
  cache hit ratio when measured, keeps waste ledger parity, and never invents savings.
- Updated UI tour, regenerated OpenAPI/TS types, and pinned behavior with API, Vitest, and
  Chromium journey coverage.
- Focused verification: regions API + facade boundaries + API/static/docs suite (`68` passed);
  UI typecheck/lint; context Vitest (`1` passed); Chromium Context journey (`1` passed). Strict
  Ruff/mypy on touched modules pass. No commit, push, tag, publish, release, or remote-setting
  change occurred.

Next dependency: T04-06 Tools analytics — normalized tool identity, invocations/success/retry/
latency/result tokens/cost share/trend, unused-schema tax, failure samples, and adapter coverage.

### 2026-07-18 — T04-06 Tools implementation checkpoint

- Added shared `server.analyze.tool_identity` classification (builtin/MCP/shell/unknown) and fixed
  the `ReadFile` → `read` alias so demo and Cursor names collapse correctly.
- New `/api/analytics/tools` response with answer-first ledger, normalized tool aggregates
  (invocations, sessions, success/error/retry, p50/p95 latency, result tokens, token-share cost),
  failure samples, adapter coverage, and mapped `tool_schema` overhead as unused-schema tax without
  per-tool invention. Static export captures the endpoint for supported `days` presets.
- Diversified deterministic demo tool_call names (Read/Bash/Edit/Grep/MCP/unknown) while preserving
  120-trace identity.
- Added production `/tools` page to the shared nav registry (13 working routes), UI tour, and
  browser route matrix.
- Focused verification: tool identity + tools API + facade + demo (`9` passed); UI typecheck/lint;
  generated OpenAPI/TS drift regenerated. Broader gates continue in the same wave.

Next dependency: T04-07 Files analytics — reads/edits/reverts/churn/cost/evidence with safe
repo-relative paths.

### 2026-07-18 — T04-07 Files implementation checkpoint

- Added `/api/analytics/files` with answer-first ledger, path hotspots (reads/re-reads/edits/
  deletes, revert/fixup sessions, token-share cost), timezone churn series, ignored-prefix flags,
  and hard scrubbing of absolute/home paths. Static export captures the endpoint.
- Diversified demo `path_rel` values across six repo-relative paths while keeping 120-trace identity.
- Added production `/files` page to the shared nav registry (14 working routes) and UI tour.
- Focused verification: path scrub/ignore tests, files API shape (drops absolute paths), facade and
  demo identity, UI typecheck/lint, generated OpenAPI/TS drift.

Next dependency: T04-08 Agents trends, fingerprint, handoff table, health/model/sample sizes.

### 2026-07-18 — T04-08 Agents implementation checkpoint

- Extended `/api/agents` with a required answer-first ledger, day×agent trend, adapter parse-health
  coverage, and additive per-agent fields (waste, quality mean/n, models/sources, fingerprint
  thumbnail, sample size). Cost remains span-token attributed; no invented leaderboard winner.
- Rebuilt `/agents` as a ledger-driven page with attributed spend, handoff table + DAG, coverage,
  and explicit limitations. UI tour § Agents updated.
- Focused verification: strict mypy on analytics payload domain; `test_agents_shape` + facade
  boundaries; regenerated OpenAPI/TS; UI typecheck/lint/format. No commit, push, tag, publish, or
  remote-setting change occurred.

Next dependency: T04-09 Compare difficulty-aware agent metrics with intervals and confound guards.

### 2026-07-18 — T04-09 Compare implementation checkpoint

- Added `/api/analytics/compare` with answer-first ledger, agent×difficulty cells (cost/tokens/
  quality/waste, retry, cost/success, verification debt, verified success, correction burden),
  anytime-valid intervals, pairwise cost-per-session views, and model/source confound warnings.
  Overall winners require n≥20, non-overlapping intervals, and no confounds; otherwise none.
- Primary agent per session is highest token-share agent. Static export captures the endpoint.
- Added production `/compare` Analyze route (nav no longer points Session Diff at “Compare”);
  Session Diff remains Sessions → Compare selected. UI tour distinguishes both.
- Focused verification: compare API shape + facade boundaries + agents shape; Ruff/mypy; generated
  OpenAPI/TS; UI typecheck/lint/format; Chromium compare journey asserted in smoke. No commit,
  push, tag, publish, or remote-setting change occurred.

Next dependency: T04-10 Behavior verdict/baseline/drift/control/evidence event context.

### 2026-07-18 — T04-10 Behavior implementation checkpoint

- Extended `/api/behavior` with a required answer-first ledger, `limitations`, and additive drift
  fields (`sample_size`, `drifted_at`, `magnitude`). Guard/instruction neighbors stay explicitly
  unavailable until T04-16.
- Rebuilt `/behavior` as a ledger-first page with drift evidence table, labeled radar + table,
  always-on EWMA, on-demand control chart, baseline progress, and first-drifted session links.
- Focused verification: behavior API shape + facade boundaries; Ruff/mypy; regenerated OpenAPI/TS;
  UI typecheck/lint; Behavior Vitest fixture updated. No commit, push, tag, publish, or
  remote-setting change occurred.

Next dependency: T04-11 Quality outcome vs process, debt/claims/calibration/labels.

### 2026-07-18 — T04-11 Quality implementation checkpoint

- Extended `/api/quality` with a required answer-first ledger separating process-quality from
  verification evidence: verified completion, verification debt, mean cost/success, lucky/unlucky
  counts. Unsupported-claim rate remains explicitly null until verification receipts land.
- Added daily trend (quality/verified/debt + human overlays), component means/weights, calibration
  coverage + human-label agreement, investigation rows, and per-outcome `verification_state`.
- Rebuilt `/quality` as a ledger-first page with interpretation limits and evidence links.
- Focused verification: quality API shape + facade boundaries; Ruff/mypy; regenerated OpenAPI/TS;
  UI typecheck/lint/format; Chromium quality journey; docs tests. No commit, push, tag, publish, or
  remote-setting change occurred.

Next dependency: T04-12 Insights prioritization, dedupe, evidence panel, snooze migration/actions.

### 2026-07-18 — T04-12 Insights implementation checkpoint

- Added migration `0008_insight_snooze.sql` (`snoozed_until`, `snooze_savings_baseline`, `see_count`)
  and `insight_snooze` action (default 14 days). Re-fires auto-unsnooze on expiry or material
  savings growth; lifecycle bumps recurrence.
- `/api/insights` now returns an answer-first ledger, rank fields (impact/confidence/recurrence/
  rank_score), duplicate suppression for shared primary evidence traces, and limitations.
- Rebuilt Insights UI with ledger, list/kanban toggle, SidePanel evidence, Ack/Snooze actions, and
  muted cards filtered from the board.
- Focused verification: insights/parity/store/facade tests; Ruff/mypy; OpenAPI/TS regen; UI
  typecheck/lint; Insights Vitest. No commit, push, tag, publish, or remote-setting change.

Next dependency: T04-13 Optimize portfolio/decay/revert/source/Guard links.

### 2026-07-18 — T04-13 Optimize implementation checkpoint

- Added migration `0009_optimize_portfolio_decay.sql` (`proposal_source`, `decay_state`,
  `last_evaluated_at`, `plain_verdict`, confound/effect history JSON, `guard_event_id`).
- Measure/revert persist plain-language verdicts, effect history, and descriptive decay labels;
  propose tags `local` vs `provider` source.
- `/api/experiments` returns an answer-first ledger, portfolio membership, write-safety and Guard
  limitation strings on detail; Overview `decayed_rules` links to portfolio when decay flags exist.
- Rebuilt Optimize UI with ledger, Board/Portfolio tabs, plain verdict before stats, sparklines,
  source/decay chips, safe revert on verdict, and honest Guard-unavailable copy.
- Focused verification: experiments/overview/demo/improve/static/parity tests; Ruff/mypy;
  OpenAPI/TS regen; UI typecheck/lint; Optimize Vitest. No commit, push, tag, publish, or
  remote-setting change.

Next dependency: T04-14 Live heartbeat/reconnect/ticks/checkpoints/backpressure/a11y.

### 2026-07-18 — T04-14 Live implementation checkpoint

- Named `heartbeat` SSE frames (plus proxy comment keepalives); client connection state
  (connecting/connected/reconnecting/stale) with silence timeout; event names aligned to
  `insight-updated` / `job-progress`.
- Live UI: ledger, auto-follow, pause buffer, polite `aria-live` summary, reduced-motion-safe
  arrival pulse, static-snapshot unavailable empty state, honest cost/checkpoint unavailable copy.
- Backpressure tests for drop-oldest queue + `dropped_events` framing; Vitest for connection/
  heartbeat/stale and event-name listeners. Cost ticks and verified-checkpoint warnings remain
  deferred to their publisher trackers (T05-05 / verification) rather than invented.
- Focused verification: `tests/test_sse.py`, UI typecheck/lint, `live.test.ts`. No commit, push,
  tag, publish, or remote-setting change.

Next dependency: T04-15 Settings tabs (workspace/appearance/budget/adapters/data/MCP/quality/privacy/about).

### 2026-07-18 — T04-15 Settings implementation checkpoint

- Replaced Settings with nine §4.16 tabs (`?tab=`): Workspace, Appearance, Budget, Adapters,
  Data, MCP, Quality, Privacy & Network, About.
- Budget/Quality edit `budgets.*` via existing `config_set`; Data rebuild uses Dialog + typed
  confirm with explicit non-delete scope; retention/backup/restore/delete/reset remain honest
  unavailable copy (no dead buttons).
- About surfaces `/health` version; Privacy documents local-first + reflector preview-consent;
  MCP install/preview status; Quality empty-state no longer invents `outcomes.enabled`.
- Focused verification: UI typecheck/lint, `settings.test.tsx`. No commit, push, tag, publish,
  or remote-setting change.

Next dependency: T04-16 Guard git instruction events, association intervals/confounds, API/static/CLI.

### 2026-07-18 — T04-16 Guard implementation checkpoint

- Migration `0010_guard_instruction_events.sql`; shared `git_local` helpers; scanner for
  AGENTS.md / CLAUDE.md / `.cursor/rules` with rename/revert/merge/dirty/no-git states.
- `/api/analytics/guard` ledger with scrubbed diffs and non-causal associations (anytime-valid
  intervals); `cairn guard`; static export; Overview `kind=guard` annotations; Optimize
  `guard_event_id` linking when windows match.
- UI `/guard` in Act nav; demo fixture; docs/guard.md + cli.md regen. Language stays
  associated-with / observed-after only.
- Focused verification: test_guard, module/CLI boundaries, demo/docs/static/parity; Ruff/mypy;
  OpenAPI/TS; UI typecheck/lint; Guard Vitest. No commit, push, tag, publish, or remote-setting
  change.

Next dependency: T04-17 Recap weekly route depth (Guard events, best/worst, recommendation, scheduling docs).

### 2026-07-18 — T04-17 Recap implementation checkpoint

- Extended `RecapResponse` with rolling-7d bounds, CPS trend, best/worst privacy-safe sessions,
  decayed rules, Guard events, ranked recommended action, and limitations.
- Recap UI/CLI render the new sections when present; `docs/recap.md` documents cron/launchd/
  Task Scheduler scheduling. No invented sessions or Guard rows.
- Focused verification: recap API/CLI money tests, docs list, OpenAPI/TS regen, UI typecheck.
  No commit, push, tag, publish, or remote-setting change.

Next dependency: T05-01 Budget burn projections and related Phase 5 trackers.

### 2026-07-18 — T05-01 Budget burn implementation checkpoint

- Shared `server/analyze/budget_burn.py`: timezone month bounds, spend-to-date, linear + trailing-7d
  projections, overrun date, agent/model shares, insufficient-history honesty gates.
- `GET /api/analytics/budget`, Overview hero reuse, Settings burn readout, `cairn stats [--json]`,
  static export of budget + overview ceilings via `load_config`. Config mutation stays on
  `config_set` only.
- Docs: configuration.md, ui-tour §1/§16; OpenAPI/TS + cli.md regen; focused tests in
  `tests/test_budget_burn.py`. No commit, push, tag, publish, or remote-setting change.

Next dependency: T05-02 Detector consolidation (retry/context/model/schema families).

### 2026-07-18 — T05-02 Detector consolidation checkpoint

- Family aggregators in `server/improve/detectors/families.py` for retry storm, context thrash,
  model mismatch, and stale tool schema; engine suppresses alias producers when a family fires.
- Shared Insight contract fields: `family`, `estimate_kind`, `confidence`, `coverage`,
  `subject_key`, `span_ids`, `alias_ids`; drafts fingerprint by subject for families.
- Context wiring: rebilling from `context_regions`, unused-tools heuristic, retry-storm cost/spans,
  per-file thrash costs, model comparable-sample counts. Multi-model no longer recommends a cheaper
  model without ≥8 matched samples.
- Docs/optimize.md + focused `tests/test_detector_families.py`. No commit, push, tag, or publish.

Next dependency: T05-03 Deterministic postmortems.

### 2026-07-18 — T05-03 Postmortem implementation checkpoint

- `server/analyze/postmortem.py` builds diagnose-cascade postmortems: degradation step, prior
  context, origin action, downstream retries/waste/cost, uncertainty, span links, Markdown.
- Additive `TraceDetailResponse.postmortem` plus `GET /api/traces/{id}/postmortem`; UI tab with
  Copy Markdown; reflector stays absent/opt-in labeled. No causal claims.
- OpenAPI/TS regen; `tests/test_postmortem.py`. No commit, push, tag, or publish.

Next dependency: T05-04 Optimize portfolio/decay opportunistic evaluation.

### 2026-07-18 — T05-04 Portfolio opportunistic re-evaluation checkpoint

- Migration `0011_optimize_eval_interval_history.sql`: `eval_interval_days`,
  `verdict_history_json`, `regression_outside_interval`.
- `reevaluate_due_experiments` refreshes decay, re-measures due portfolio rules, preserves
  historical verdicts, flags estimates outside the prior CI (descriptive). Wired on sync, recap,
  and `optimize_evaluate` / `cairn optimize evaluate` — no daemon.
- Experiments API/UI expose last evaluated, interval, sample size, outside-interval chip, and
  verdict history. Docs/optimize.md updated. Focused tests in `tests/test_optimize_reeval.py`.
  No commit, push, tag, or publish.

Next dependency: T05-05 SSE coalesced `session_cost_tick` events.

### 2026-07-18 — T05-05 session_cost_tick checkpoint

- `SessionCostTickPublisher` on `EventBus.cost_ticks`: ≤1 tick / 2s / session, burst coalesce to
  latest absolute totals, duplicate-total suppression, timer + `flush_due`, measured/estimated/
  unavailable from `cost_source`. Wired from adapter ingest and OTLP insert.
- Live UI listens for `session_cost_tick`, shows a sidebar ticker, and updates `aria-live` only on
  a periodic summary (not every tick). Docs/api + ui-tour updated. Tests in
  `tests/test_cost_ticks.py`. No commit, push, tag, or publish.

Next dependency: T05-06 MCP `cairn_context_budget`.

### 2026-07-18 — T05-06 MCP context budget checkpoint

- Added `cairn_context_budget` without changing existing tool contracts: composition, largest
  removable/stale regions, one conservative trim suggestion, `data_as_of` / `estimate_status`,
  explicit `trace_id` on ambiguity, read-only / no provider call, consultation marker.
- Docs in `docs/mcp.md`; tests in `tests/test_mcp_context_budget.py` (plus list_tools count).
  No commit, push, tag, or publish.

Next dependency: T05-07 CLI stats/top/why/guard.

### 2026-07-18 — T05-07 CLI stats/top/why/guard checkpoint

- `cairn stats|guard --json` emit versioned envelopes (`cairn.stats.v1` / `cairn.guard.v1`).
- Added `cairn top` (`--once`, `--interval`, non-TTY single shot, clean Ctrl-C) and `cairn why`
  (deterministic postmortem Markdown/JSON; exit 1 missing, 2 no evidence).
- `scripts/gen_cli_docs.py` documents JSON schemas and exit codes. Tests in
  `tests/test_cli_top_why.py`. No commit, push, tag, or publish.

Next dependency: T05-08 shareable scrubbed session HTML.

### 2026-07-18 — T05-08 session HTML export checkpoint

- `cairn export session <trace_id> --html` writes self-contained CSP HTML (no scripts) with
  scrubbed waterfall, transcript, postmortem, and evidence summary via action
  `export_session_html`. Size warnings for large traces; hostile greps in
  `tests/test_session_html_export.py`. Legacy `cairn export` bundle/static preserved.
  No commit, push, tag, or publish.

Next dependency: T05-09 verification engine.

### 2026-07-18 — T05-09 verification receipt v1 checkpoint

- Deterministic `cairn.receipt.v1` builder from outcomes/spans: status, intent, requirements,
  empty claims + limitation, evidence refs, timeline, transparent debt weights, content hash.
- Migration `0012_verification_receipts.sql`, `GET /api/traces/{id}/receipt`, detail embed,
  static export capture, `verification_rebuild` action, `cairn receipt [--json]`.
- Session Receipt UI consumes the API; `claim:unsupported` remains unavailable until claim
  extraction exists. Tests in `tests/test_verification_receipt.py`. No commit, push, tag, or publish.

Next dependency: T05-10 regression artifact system.

### 2026-07-18 — T05-10 regression artifact system checkpoint

- Versioned `cairn.regression.v1` under `.cairn/regressions/<id>/` with scrubbed task, commit ref,
  empty setup commands, inferred-only verification hints, expected outcome, privacy inventory.
- CLI: `cairn regression create|ls|show|validate|export|import|delete` — no command execution.
- Actions: `regression_create|delete|export|import`. Hostile zip import rejects traversal/symlinks;
  attachments skipped on import. Docs: `docs/regressions.md`. Tests:
  `tests/test_regression_artifacts.py`. Run/compare deferred (schema `runs` stays empty).
  No commit, push, tag, or publish.

Next dependency: T05-11 corrections / supervision / handoff.

### 2026-07-18 — T05-11 corrections / handoff checkpoint

- Conservative phrase classifier (`cairn.corrections.v1`) with recovery linkage, local relabel
  table, `corrected:` filter, `corrections_rebuild` action, Session Corrections UI.
- Offline handoff capsule (`cairn.handoff.v1`) via API/CLI/MCP with fact/inference/recommendation
  tags, scrubbing, and char budget. Tests: `tests/test_corrections_handoff.py`. No commit, push,
  tag, or publish.

Next dependency: T05-12 advisory policies / review risk.

### 2026-07-18 — T05-12 advisory policies checkpoint

- Typed `[policy]` config (`path_risks`, `commands`, `required_checks`, exceptions, thresholds).
- Evaluator tags `enforcement_source` (observed/advisory/allowlisted); never claims blocking.
- Wired into receipt `risk_policy`, `cairn check` failures, and `risk:high` filter.
- Docs: `docs/policy.md`. Tests: `tests/test_policy.py`. No commit, push, tag, or publish.

Next dependency: T05-13 MCP evidence tools.

### 2026-07-18 — T05-13/14/15 evidence workflow checkpoint

- MCP: `cairn_verification_status`, `cairn_policy_check`, `cairn_regression_context`,
  `cairn_next_evidence` (+ existing handoff/context_budget). Read-only, consultation markers.
- CLI: `cairn review`, `cairn verify next`, `cairn resource --json`, `cairn privacy --json`
  (archive deferred to T06-12 with full portable format).
- Next-evidence preview shares approval_class / side_effects / estimated_cost heuristics;
  never executes. Tests: `tests/test_mcp_evidence.py`. No commit, push, tag, or publish.

Next dependency: Phase 6 resource shield (T06-01).

### 2026-07-18 — T06-01 collection modes checkpoint

- `[collection].mode` = `manual` | `efficient` | `live` with honest status separating auto-sync
  from browser SSE Live updates and Sync now.
- Manual starts no watcher/refresh threads; Efficient/Live tune poll + rediscovery intervals;
  `config set collection.mode` reloads the running pipeline.
- Tests: `tests/test_collection_modes.py`. No commit, push, tag, or publish.

### 2026-07-18 — T06-02 resource contracts / disk inventory checkpoint

- Shared `server/util/resources.py`: category inventory (db/WAL/exports/backups/regressions/…),
  soft budget warn/over, descriptive span-based growth forecast, process RSS sample.
- CLI `cairn resource[--json]` emits `cairn.resource.v1`; workspace embeds `resources`;
  Overview/session Resource shields use measured inventory; doctor checks soft budget.
- Config: `[resources].soft_budget_bytes`. Docs: `docs/resource-shield.md`.
  Tests: `tests/test_resources.py`. No commit, push, tag, or publish.

### 2026-07-18 — T06-03 watcher coalescing checkpoint

- Single queue event path (no dual callback+queue ingest); path coalescing; burst debounce;
  idle poll backoff; per-cycle path/change caps; missing-path prune after N scans.
- Pipeline status exposes measured `watcher.*` scan facts (`event_path: "queue"`).
- `stop()` always delivers the iterator sentinel (even when the queue is saturated).
- Tests: `tests/test_watcher.py`. Docs: `docs/architecture/watcher.md`. No commit, push, tag, or publish.

### 2026-07-18 — T06-04 bounded jobs checkpoint

- `JobRunner` uses a ThreadPoolExecutor with queue cap, per-action dedupe (sync/backfill),
  cooperative cancel, progress SSE, result TTL prune, soft timeout, and lifespan shutdown.
- API: `GET /api/actions/jobs/{id}`, `POST …/cancel`; saturation → HTTP 429.
- Config `[jobs].*`. Docs: `docs/architecture/jobs.md`. Tests: `tests/test_jobs.py`.
  No commit, push, tag, or publish.

### 2026-07-18 — T06-05 storage modes checkpoint

- `[storage].mode` = metrics | balanced | forensic with truncate/scrub-at-ingest;
  ingest applies policy in `IngestWriter`; upgrades require `confirm_storage_upgrade`.
- Action `storage_strip` nulls `text_inline` resumably (hashes/metrics kept).
- `cairn privacy` reports storage status. Docs: `docs/storage-modes.md`.
  Tests: `tests/test_storage_modes.py`. No commit, push, tag, or publish.

### 2026-07-18 — T06-07 git privacy checkpoint

- `server/analyze/git_privacy.py`: ignore/tracked assessment, approved `.git/info/exclude`
  write, export tracked-path warnings; doctor `Git privacy` check; action `git_exclude_cairn`.
- FirstRun offer + `cairn privacy` git section. Tests: `tests/test_git_privacy.py`.
  No commit, push, tag, or publish.

### 2026-07-18 — T06-08 lifecycle checkpoint

- `server/store/lifecycle.py`: plan/cleanup (strip_text | delete_traces), backup/restore,
  integrity, compact; source agent logs never touched; warn-only until
  `[lifecycle].destructive_enabled`.
- Actions: `lifecycle_plan`, `lifecycle_cleanup`, `db_backup`, `db_restore`,
  `db_integrity`, `db_compact`. `cairn privacy` reports lifecycle + integrity.
- Docs: `docs/data-lifecycle.md`. Tests: `tests/test_lifecycle.py`.
  No commit, push, tag, or publish.

### 2026-07-19 — T06-11 offline pricing checkpoint

- Bundled `model_prices.json` carries `source` / `version` / `effective_date`.
- `pricing_status` + `pricing_kind` (matched/override/unknown); `stale_after_days` config;
  doctor + Overview `stale_pricing` note; `pricing_refresh_preview` never downloads.
- Docs: `docs/pricing.md`. Tests: `tests/test_pricing.py`.
  No commit, push, tag, or publish.

### 2026-07-19 — T06-12 portable archive checkpoint

- `server/archive/`: `cairn.archive.v1` ZIP (manifest/checksums/privacy/domains),
  modes full|scrubbed|metadata_only; inspect; import dry-run + conflict policy;
  path-safe/bomb-resistant ZIP reader (ADR-10).
- Actions/CLI: `archive_export` / `archive_import` / `archive_inspect` +
  `cairn archive …`. Docs: `docs/archive.md` + OTLP-loss in `docs/otlp.md`.
- Tests: `tests/test_archive.py`. `export_bundle` facade unchanged.
  No commit, push, tag, or publish.

### 2026-07-19 — T06-13 egress ledger checkpoint

- `server/util/egress.py`: secret-free JSONL ledger (origin/purpose/field classes/bytes/consent);
  wired into reflector provider HTTP calls; preview does not record.
- Actions `egress_status` / `egress_export`; `cairn privacy` + doctor surfaces; docs
  `docs/egress.md`. Tests: `tests/test_egress.py` + empty-ledger assertion in
  `tests/test_no_network.py`. No commit, push, tag, or publish.

### 2026-07-19 — T06-14 circuit breakers checkpoint

- Pipeline gate: `max_file_bytes` / `max_parse_ms` / failure streak pause / soft-budget
  global pause; quarantine manifests under `.cairn/quarantine/` (source untouched).
- Actions `circuit_status` / `circuit_resume`; shield states degraded/paused/quarantined.
- Docs: `docs/resource-shield.md`. Tests: `tests/test_circuit_breakers.py`.
  No commit, push, tag, or publish.

### 2026-07-19 — T06-15 reference mode checkpoint

- `[storage].mode = "reference"`: no `text_inline`; source logs authoritative; honest
  non-zero-copy list for hashes/metrics; drift JSONL on missing/shorter rewrite.
- Pipeline records drift in reference mode; action `source_drift_status`.
- Docs: `docs/storage-modes.md`. Tests: `tests/test_reference_mode.py`.
  No commit, push, tag, or publish.

### 2026-07-19 — T07-01 evidence-led README checkpoint

- README rewritten to ≤250 lines: outcome hero, real media (hero.gif + v1.2 baseline/
  session/optimize screenshots), 60s install, differentiators, adapters + accuracy caveat,
  privacy/honest limits, CLI table, architecture thumbnail, Why Cairn comparison links.
- Version badge stays `1.1.1` (matches `pyproject.toml`); no release claim of 1.2.0.
- Added lean `docs/verification.md` so README receipt link resolves.
  No commit, push, tag, or publish.

### 2026-07-19 — T07-02 required docs set checkpoint

- Added missing required pages: `docs/privacy.md`, `docs/releasing.md` (pointer to `release.md`),
  `docs/research/competitive-landscape.md`, `docs/research/user-needs.md`; indexed in
  `docs/README.md`. Existing verification/regressions/resource/security/perf/a11y/maintainer docs
  retained. No commit, push, tag, or publish.

### 2026-07-19 — T07-03 Pages / base-path checkpoint

- Static export writes `.nojekyll` + `404.html`; HTTP test serves under `/Cairn/` prefix.
- `demo-pages.yml`: PR preview artifact; deploy remains `workflow_dispatch`.
- Canonical demo links in README/`docs/README.md`; `docs/pages.md` hosting contract.
  Maintainer homepage checkbox stays unchecked until live verify. No commit/push/tag/publish.

### 2026-07-19 — T07-04 examples checkpoint

- Replaced retired pipeline e2e-demo (cairn build / Ollama) with provider-free demo walkthrough.
- Added ci-gate, otlp-ingest (shared fixture), mcp-setup, export-archive examples + index.
- CI: `tests/test_examples.py` (config load, setup.sh, demo check, OTLP parity).
  No commit, push, tag, or publish.

### 2026-07-19 — T07-05 CONTRIBUTING checkpoint

- Replaced short guide with bootstrap, repo map, test matrix, generated-file/deps policy,
  screenshot workflow, migration/API/a11y/privacy checklists, adapter + PR guidance, release
  pointers. `tests/test_docs.py` green. No commit, push, tag, or publish.

### 2026-07-19 — T08-01 Python acceptance checkpoint

- Full suite green: `477 passed`; Ruff + format + strict mypy clean.
- Fixed stale parity/CLI/migration/API expectations; regenerated OpenAPI/CLI artifacts;
  typing/format cleanup. Report: `docs/plans/v1.2.0-acceptance-python.md`.
  No commit, push, tag, or publish.

### 2026-07-19 — T08-02 UI Vitest acceptance checkpoint

- UI gates green: typecheck/lint/format/check:api; Vitest **93 passed**.
- Prettier cleanup; receipt/corrections component test added.
  Report: `docs/plans/v1.2.0-acceptance-ui.md`. No commit, push, tag, or publish.

### 2026-07-19 — T08-03 Playwright acceptance checkpoint

- Browser suite green: **91 passed** (Chromium full + Firefox/WebKit `@cross-browser`).
- Fixed Compare a11y scroll regions; aligned smoke/static assertions with current UI copy.
  Report: `docs/plans/v1.2.0-acceptance-browser.md`. No commit, push, tag, or publish.

### 2026-07-19 — T08-04 performance acceptance checkpoint

- Stable budgets green after static IIFE ceiling 230→240 KiB; structural perf tests passed.
- Refreshed large-profile timing JSON; medium + large local runs recorded.
  Report: `docs/plans/v1.2.0-acceptance-performance.md`. No commit, push, tag, or publish.

### 2026-07-19 — FIN-01 release gate checkpoint

- `scripts/release_check.py` is the single full local gate (quality + packaging + reproducibility).
- CI/publish use `--packaging-only`. Full gate passed locally (~7.5m).
  Report: `docs/plans/v1.2.0-acceptance-release-gate.md`. No commit, push, tag, or publish.

### 2026-07-19 — FIN-02 final evidence report checkpoint

- Wrote 19-part report: `docs/plans/v1.2.0-final-report.md` (links acceptance evidence,
  deferred P1/P2, maintainer-only settings, no tag/push/publish confirmation).
- Honest non-claim: package version remains 1.1.1 until maintainer release PR + annotated tag.
  No commit, push, tag, or publish.

**Program status:** Implementation + acceptance + release gate + final report complete for the
in-tree 1.2.0 work. Remaining / partial P1: T03-02 (manual AT receipt blank), T06-06 (CAS
deferred via ADR). T05-10/T06-09/T06-10 closed with honesty limits (record-only run/compare,
typed restore UI, accelerated soak v2). EXP/RES deferred via hypothesis cards.

### 2026-07-19 — T06-06 content-addressed assessment checkpoint

- ADR 0013 defers CAS for 1.2.0; FTS stays retired (ADR 0012). Hashes + storage modes remain.
- `server/store/dedup_assessment.py` + `scripts/benchmark.py assess-dedup` measure reuse/savings.
- Tests: `tests/test_dedup_assessment.py`. Docs: `docs/performance.md`. No commit/push/tag/publish.

### 2026-07-19 — T06-09 Resource & Privacy Center checkpoint

- Settings → Resource & Privacy wires disk inventory, storage mode, strip, lifecycle dry-run,
  backup/list/restore, integrity/compact, git exclude, egress, and circuit status.
- Restore: list backups → dry-run preview → type `RESTORE` + destructive flag. No FTS rebuild.
  Docs: `docs/resource-shield.md`. Tests: lifecycle + settings. No commit/push/tag/publish.

### 2026-07-19 — T06-10 accelerated resource soak checkpoint

- `server/store/resource_soak.py` v2 samples idle RSS, EventBus load, job-action latency, and a
  collection-mode flip (accelerated; not an 8h or HTTP-SSE claim).
- Test: `tests/test_resource_soak.py`. Docs: `docs/performance.md`. No commit/push/tag/publish.

### 2026-07-19 — T03-02 a11y receipt template checkpoint

- Automated axe/E2E evidence remains in acceptance-browser + `docs/accessibility.md`.
- Added maintainer manual AT receipt: `docs/plans/v1.2.0-a11y-manual-receipt.md`.
- Status is **partial**: automation complete; VoiceOver/NVDA/JAWS matrix blank until a
  maintainer fills the receipt (cannot be honestly closed by automation alone).

### 2026-07-19 — P2 EXP/RES hypothesis cards checkpoint

- Wrote `docs/plans/v1.2.0-hypothesis-cards.md` for EXP-I…N and RES-P…R.
- Tracker status set to **deferred** (post-1.2.0); no experimental production features landed.
  No commit/push/tag/publish.

### 2026-07-19 — Remaining P1 pride closeout

- T05-10: `regression run` / `compare` record from ingested traces only (`executed_commands=false`);
  definition `content_hash` excludes runs. Tests: `tests/test_regression_artifacts.py`.
- T06-09: Resource Center backup list → dry-run → typed `RESTORE`; `db_backup_list` +
  `db_restore(dry_run=…)`. Tests: `tests/test_lifecycle.py`.
- T06-10: `cairn.resource_soak.v2` idle / EventBus / jobs windows (still accelerated).
- DataTable adopted on Tools (Sessions remains purpose-built). Docs: regressions / resource-shield /
  performance / ui-primitives. T03-02 stays partial (manual AT receipt). No commit/push/tag/publish.

### 2026-07-19 — Pride / honesty closure pass

- Analytics aggregates hard-capped (`fetch_capped` + truncation `limitations`); recap extremes
  use SQL `LIMIT 1` instead of full cost lists; compare capped too.
- Analyze pages share `Stat` with `detail` card grammar; `DataTable` adopted on Tools;
  UI primitives doc matches production adoption.
- `release_check` success line names mode; CI forbids `--skip-*`; integration job renamed to
  packaging/wheel doctor. T03-02 marked **partial** (automation done, AT receipt open).
- Rebuild `view=all` rebuilds every `VIEW_ORDER` view; optimize `--llm/--apply` rejected;
  storage upgrade confirm + circuit summary in Resource Center; FirstRun force rescan.
  No commit/push/tag/publish.

