# T08-03 Playwright multi-browser acceptance report

Date: 2026-07-19

## Commands

```bash
npm --prefix ui run test:e2e
```

WebServer: `scripts/build_ui.py build` + `scripts/e2e_server.py` (deterministic seed).  
Projects: Chromium (full), Firefox/WebKit (`@cross-browser` only).

## Results

| Engine | Scope | Result |
|--------|-------|--------|
| Chromium | smoke + accessibility (dark/light × routes) + static `file://` | pass |
| Firefox | `@cross-browser` overview, custom range, session detail, static | pass |
| WebKit | same as Firefox | pass |
| **Total** | | **91 passed** |

## Coverage vs §8.3 (honest map)

| Requirement | Status |
|-------------|--------|
| Deterministic seeded server (no skip) | Yes (`e2e_server.py` / health gate) |
| First-run / empty / demo path | Smoke + first-run journeys |
| Routes dark + light a11y | `accessibility.spec.ts` axe WCAG 2.2 AA tags |
| Keyboard / skip / palette focus trap | Accessibility + smoke |
| Custom range + typed filters | Smoke |
| Session detail / replay / transcript / inspector | Smoke `@cross-browser` |
| Receipt / debt | Smoke receipt tab |
| Compare / Guard / Optimize / Settings | Route renders + page-specific smoke |
| Static `file://` export | `static.spec.ts` cross-browser |
| Mobile viewport | Sessions phone fit |
| Theme bootstrap / OS follow | Smoke theme tests |

Not every §8.3 bullet has a dedicated end-to-end scenario (e.g. full regression create UI,
SSE reconnect stress). Those remain covered at unit/API layers or are tracked as follow-ons;
the mandatory CI browser gate is green and non-skipping.

## Fixes this wave

- Compare scroll regions: `tabIndex={0}` + labeled `role="region"` (axe `scrollable-region-focusable`).
- Smoke/static assertions updated to current receipt/postmortem/recap copy.
- Theme preference test opens Settings → Appearance tab before asserting buttons.
- Recap kicker notes `generated locally`.

## Follow-ons

- T08-04 performance matrix.
- Optional deeper journeys: regression create/validate UI, SSE reconnect, Optimize apply/revert UI.
